# Defect register

As of 2026-09-07, HEAD `814a1b33f0ec816177009ed4c4e02e83a9edd850` on `master`. Compiled while the audio module's
rename from `AudioScribe` to `ascend-audio-scribe` was completing live in a concurrent session, so any entry below
that touches that module names it as observed at verification time, not as a promise the path is still current by
the time you read this.

Entries A22 through A32 were added later the same day at HEAD `16cfc60`, from the first full end-to-end sweep
of all six modules. Every one of them was reproduced directly rather than taken from a runner's report, and
each says which measurement settled it.

Rows A36 through A55 were added from the 2026-09-09 and 2026-09-10 sweeps and closed the same day at HEAD
`df9f0a7eabe3ac56917649c54334b0ad7e52e524`, each moved to Fixed with the commit or commits that carried its fix, or to
Currently dormant where it was an observation with no fix.

### Why this exists

Bugs were being found and fixed across several sessions with nothing written down. The ones somebody acted on lived
only in a chat transcript; the ones nobody acted on existed nowhere at all. This register exists so the second kind
cannot be lost. Every entry below was checked against the running repository (source, tests, docs, CI workflows, and
`.git/logs/HEAD`) rather than accepted on say-so. Where that check could not be completed with the tools available in
this session, the entry says so plainly instead of guessing.

### How to maintain it

Add an entry the moment a defect is found, whether or not it gets fixed in the same sitting. Move it to Fixed with a
commit reference the moment a fix lands, not before. An entry with no commit is not fixed, no matter how confident
the report. When a Found and not fixed entry is closed, move it to Fixed rather than deleting it, since the register
proves the fix landed, not just that a bug once existed.

Prune Fixed entries older than roughly two release cycles once the corresponding code has survived without
regression, unless the entry documents a pattern worth remembering (the several path-anchored-to-old-name defects
below are a pattern, not one bug, and are worth keeping longer). Never prune Found and not fixed or Declined entries;
those are exactly the ones a stale document would let disappear.

### Reading paths

Operator about to rename a module or cut a release: read Actionable now and Actionable, dormant in full before you
start.
Anyone reviewing the OCR service: read the OCR request-budget block under Fixed, including its deployment caveat,
before assuming the fix is live in production.
Owner triaging what to do next: read Actionable now top to bottom, then Declined, then skim Fixed for commit
references if you need to confirm one.

### Verification notes: entries that turned out to be wrong

Two entries reported as "found and not fixed" are not currently reproducible. Both are recorded below under
Actionable now with their corrected status rather than silently dropped, per the instruction that a wrong entry gets
recorded as wrong, not deleted.

The claim that `.gitignore` has ignore rules anchored to old directory names, including the one keeping scraped page
content out of the repository, does not hold against the current file. Every module-scoped rule in
[.gitignore](../.gitignore) already uses the current directory names, including `apps/ascend-web-hunter/src/storage/`,
`apps/ascend-web-hunter/.crawlee_storage/`, and, as of this session, `apps/ascend-audio-scribe/e2e/testing/runs/*`. The most
likely explanation is that the concurrent rename session updated it during this verification; either way, there is
nothing to fix in it right now.

The claim that ascend-ocr's end-to-end README claims six specifications where twelve exist does not hold either.
[apps/ascend-ocr/e2e/README.md](../apps/ascend-ocr/e2e/README.md) and [apps/ascend-ocr/README.md](../apps/ascend-ocr/README.md) both say
twelve, correctly, and so does [.github/workflows/e2e.yaml](../.github/workflows/e2e.yaml) ("ascend-ocr (12 of
12 free specs)"). No file anywhere under `apps/ascend-ocr/` was found claiming six.

One entry in Fixed could not be pinned to a specific recent change and is flagged there rather than here: the
"compatibility patch dead against its own pinned dependency" most plausibly maps to AscendMemory's
[ADR-006](../apps/ascend-memory/docs/architecture/decisions/ADR-006-mem0ai-2x-upgrade.md), but that ADR is dated
2026-06-01, roughly three months before the window this register otherwise covers, and describes a patch retired by
a deliberate major-version upgrade rather than one found silently dead. See its entry below for the full caveat.

---

## Actionable now

Ordered by whether the defect is already biting or only waiting for a trigger. This block first.

### Currently active

| # | Defect | Where | Effect | Trigger |
| :- | :--- | :--- | :--- | :--- |
| A1 | Bruno request bodies for four ascend-ocr specs hardcode an absolute Windows path | [docs/api/request/AscendAI/ocr/ocr.yml:18](../docs/api/request/AscendAI/ocr/ocr.yml), `testing/ocr-default-lang.yml:18`, `testing/ocr-unsupported-mime.yml:18`, `testing/ocr-polish.yml:18`, all `D:\Development\projekty-IT\AscendAI\ascend-ocr\e2e\fixtures\...` | These four specs (English OCR, default-language OCR, unsupported-MIME rejection, Polish OCR) cannot resolve the fixture file on any machine but this one, including the `ubuntu-latest` runner [.github/workflows/e2e.yaml](../.github/workflows/e2e.yaml) already dispatches them against nightly | Already active. The nightly and on-push e2e workflow runs these specs against a hosted runner today and they fail there |
| A2 | `.gitattributes` anchors `/gradlew text eol=lf` to the repository root | [.gitattributes:1](../.gitattributes) | Matches nothing. The only `gradlew` files in the repo are `apps/ascend-agent/gradlew` and `apps/ascend-weather-mcp/gradlew`; a leading `/` in a root `.gitattributes` anchors to the directory containing that file, not to any nested one | Already active, unrelated to any module move. Nobody's `gradlew` gets its line-ending rule enforced |
| A3 | docling-serve peaked at 7755 MiB against its own 8 GiB compose limit | [compose.yaml:23](../compose.yaml), measured in [docs/architecture/memory-budget.md](architecture/memory-budget.md) "Limits that look wrong, and the evidence" | 95 percent of its own ceiling before any interaction with ascend-ocr is even counted. Of every capped service in the stack, this is the one most likely to be killed by its own limit rather than by the host | Already active; watch it, raise the limit or lower `pdf-parallel-pages` concurrency, see the same section for the trade |
| A4 | Qdrant has no memory limit anywhere in this repository | [docs/architecture/memory-budget.md](architecture/memory-budget.md) "Per-service inventory: external prerequisites" | Its own peak went from 222 MiB to 1954 MiB, nearly ninefold, in one day on this host, and per ADR-M003 it is a deliberately unmanaged external prerequisite so nothing in this repo bounds it | Already active and already growing; the growth mechanism (collection size at RAM-mode creation) is understood, the ceiling is not |
| A5 | `.claude/settings.local.json` allowlists a container name and a module path that no longer exist | `.claude/settings.local.json:93,107` (gitignored, not tracked; see [.gitignore:49](../.gitignore)) | Line 93 is `docker cp AscendWebSearch/e2e/harness/* ascend-web-search:/tmp/*`, line 107 execs into container `ascend-web-search`. Both the module path and the container name predate the rename to `ascend-web-hunter` | Already active. Harmless on its own (a stale allow-list entry just never matches), but confusing to anyone reading the file to understand what an agent is permitted to touch |
| A6 | `apps/ascend-memory/skills/ascend-memory/SKILL.md` and `.agents/skills/ascend-memory/SKILL.md` have drifted apart | [apps/ascend-memory/skills/ascend-memory/SKILL.md](../apps/ascend-memory/skills/ascend-memory/SKILL.md) vs [.agents/skills/ascend-memory/SKILL.md](../.agents/skills/ascend-memory/SKILL.md) | Same content, different formatting conventions: one uses `##` headings with no section dividers and a plain prose style, the other uses `###` headings with `---` dividers per the `markdown-writer` skill's own rules. Nothing keeps the two synchronised | Already active. Whichever copy an agent happens to load first is the one whose conventions it will imitate next |
| A11 | ascend-ocr's configuration doc undercounts the hops to the repository root | [apps/ascend-ocr/docs/CONFIGURATION.md:94](../apps/ascend-ocr/docs/CONFIGURATION.md), `[.gitignore](../.gitignore)` | The file sits three directories under the root (`apps/ascend-ocr/docs/`) and the link climbs only one, landing on a non-existent `apps/ascend-ocr/.gitignore` instead of the real root `.gitignore`. Confirmed pre-existing: the same one-level link was already wrong at the pre-move location `ascend-ocr/docs/CONFIGURATION.md` (two directories under root, needing two ups, not one), so the 2026-09-07 move did not cause this, only added a third missing level | Already active. Anyone following the link from rendered Markdown gets a 404 |
| A12 | ascend-weather-mcp's README documents a class file that was renamed years ago | [apps/ascend-weather-mcp/README.md:110](../apps/ascend-weather-mcp/README.md), `[provider/ToolProvider.java](src/main/java/com/lukk/ascend/ai/mcp/weather/provider/ToolProvider.java)` | The file is `provider/WeatherToolConfig.java` on disk and has been since a pre-AscendAI-monorepo commit; no `ToolProvider.java` exists anywhere in the module's git history at this path. Unrelated to the 2026-09-07 restructure | Already active. The link 404s and the prose names a file that was never at this path under its current name |
| A13 | ascend-web-hunter's README links its License badge and its closing "MIT. See LICENSE." line to a per-module `LICENSE` file that has never existed | [apps/ascend-web-hunter/README.md:9,172](../apps/ascend-web-hunter/README.md) | Both links resolve to `apps/ascend-web-hunter/LICENSE`, which has no git history at any path this module has ever occupied (`AscendWebSearch/LICENSE` and `ascend-web-hunter/LICENSE` both have zero history too). The repository's only `LICENSE` file is at the root; every sibling README (e.g. [apps/ascend-ocr/README.md:290](../apps/ascend-ocr/README.md)) correctly points there via `../../LICENSE`. Predates the 2026-09-07 restructure | Already active. Two dead links in the module's own front door |
| A14 | Three root docs point at a skills location and a set of `.example` template files that do not exist under any name they name | [docs/AGENTS-UPDATE.md:14,89-90](../docs/AGENTS-UPDATE.md), [docs/AGENT_TOOLING.md:58,65-66](../docs/AGENT_TOOLING.md), [docs/MCP_SETUP.md:15,17](../docs/MCP_SETUP.md) | All three link `../.opencode/skills`, `../AGENTS.md.example`, `../kilo.jsonc.example`, `../opencode.json.example`, and `../.mcp.json.example`. `.opencode/skills/` has never existed; skills live only at `.agents/skills/` (`.opencode/agents` is a symlink to `.agents/agents/`, but `.opencode/skills` is not a symlink to anything). No `*.example` file exists anywhere at the repository root today. Unrelated to the apps/ move; this is residue of a separate `.opencode` → `.agents` consolidation and a template-file removal, neither in this task's scope | Already active. All five links 404 |
| A15 | Two links inside one OpenSpec change's delta spec count the wrong number of directories to the repository root, independent of the 2026-09-07 move | [openspec/changes/add-mcp-startup-tolerance/specs/mcp-startup-resilience/spec.md:84,105](../openspec/changes/add-mcp-startup-tolerance/specs/mcp-startup-resilience/spec.md) | The file sits five directories under the root. Both links climb only three (`../../../.agents/skills/coding-standards/SKILL.md` and `../../../apps/ascend-agent/src/main/resources/application.yaml`), landing inside `openspec/changes/`. The move commit (`251c859`) touched line 105 to rename `AscendAgent` to `apps/ascend-agent` but did not fix the up-count, which was already wrong beforehand at two directories short, not one, so this predates and is independent of the move | Already active. Both links 404 |
| A16 | One link inside another OpenSpec change's delta spec is one directory short of the repository root | [openspec/changes/stop-ocr-getting-stuck-on-large-jobs/specs/ocr-memory-bounds/spec.md:17](../openspec/changes/stop-ocr-getting-stuck-on-large-jobs/specs/ocr-memory-bounds/spec.md) | The file sits five directories under the root; the link climbs four (`../../../../apps/ascend-ocr/docs/architecture/decisions/ADR-006-detector-input-bound.md`), landing on `openspec/apps/...`. Traced through git history: the pre-move version already climbed only four to reach `ascend-ocr/docs/...` when it needed five, so this predates the 2026-09-07 move, which only kept the same (still-wrong) depth while updating `ascend-ocr` to `apps/ascend-ocr` | Already active. The link 404s |
| A17 | Two run-record templates in an old OpenSpec change reference the wrong sibling spec | [openspec/changes/add-web-search-scraping-e2e/7-authenticated-realworld-scraping-tasks.template.md:3](../openspec/changes/add-web-search-scraping-e2e/7-authenticated-realworld-scraping-tasks.template.md), [openspec/changes/add-web-search-scraping-e2e/tasks-template.md:3](../openspec/changes/add-web-search-scraping-e2e/tasks-template.md) | The first climbs one directory it should not (`../7-authenticated-realworld-scraping-test.md` from a file that already sits beside its spec, so it should read `7-authenticated-realworld-scraping-test.md`). The second points at `../6-tiered-scraping-test.md`, a file that has never existed anywhere in this change folder. Neither touches `apps/`; both predate and are independent of the 2026-09-07 move | Already active. Both links 404 |
| A18 | The monorepo observability guide describes two exporters and a container count that do not match `compose.yaml` | [docs/OBSERVABILITY.md:15,275-287](../docs/OBSERVABILITY.md) | States "eight observability containers" and lists `postgres-exporter` and `redis-exporter` in the resource-usage table, alongside a `/metrics` scrape claim for both in the "What Is Collected" section. `compose.yaml` defines no service named `postgres-exporter` or `redis-exporter` anywhere; the real observability service count is seven (`container-metrics-exporter`, `prometheus`, `grafana`, `loki`, `vector`, `otel-collector`, `tempo`), matching [infra/observability/README.md](../infra/observability/README.md)'s own table. Unrelated to the apps/ move | Already active. A reader sizing the observability stack budgets for two exporters and one more container than actually run |
| A19 | The monorepo ADR index is missing an entry | [docs/architecture/decisions/README.md:23-36](architecture/decisions/README.md) | Lists ascend-ai-agent's ADR-001 through ADR-008 only. [ADR-009](../apps/ascend-agent/docs/architecture/decisions/ADR-009-docling-bounded-retry-fanout-cap.md) exists on disk, dated 2026-09-03 (four days before the 251c859 move commit), and is not in the table. Unrelated to the apps/ move | Already active. Anyone reading the index for the full ADR set misses one |
| A20 | Three files claim the ascend-ai-agent e2e suite has five specs; it has eleven | [AGENTS.md:231](../AGENTS.md), [apps/ascend-agent/AGENTS.md:38](../apps/ascend-agent/AGENTS.md), [apps/ascend-agent/README.md:259](../apps/ascend-agent/README.md) | All three say "Five numbered specs (`1-weather-mcp` through `5-rag`)". [apps/ascend-agent/e2e/testing/](../apps/ascend-agent/e2e/testing/) holds specs 1 through 11 (`1-weather-mcp-test.md` through `11-compaction-idempotency-test.md`), and both [apps/ascend-agent/e2e/README.md:224](../apps/ascend-agent/e2e/README.md) ("eleven specs") and [docs/E2E_COST.md](../docs/E2E_COST.md)'s reconciled counts agree on eleven. The count went stale as specs 6 through 11 were added over time; unrelated to the apps/ move | Already active in three places at once. Anyone reading only the module-level docs plans for a fifth of the actual suite |
| A21 | The memory-budget document contradicts the standalone bundle's own README on what the bundle runs | [docs/architecture/memory-budget.md:134,145](architecture/memory-budget.md) | States the bundle runs "only four containers: ascend-web-hunter, searxng, flaresolverr, and the ngrok tunnel" and calls Redis "this bundle's one external prerequisite". [apps/ascend-web-hunter/deploy-standalone/README.md:13,38](../apps/ascend-web-hunter/deploy-standalone/README.md) documents five containers with Redis bundled and running unconditionally by default (a fifth container, not external), and [apps/ascend-web-hunter/AGENTS.md:57](../apps/ascend-web-hunter/AGENTS.md) independently confirms Redis is "bundled and runs unconditionally in `deploy-standalone/`". Unrelated to the apps/ move | Already active. A reader sizing the standalone host under-counts by one container and misreads where its session cache actually lives |

| A22 | The OCR service's REST upload endpoint ignores the language the caller asks for | [apps/ascend-ocr/src/api/rest/rest_endpoints.py:32,35](../apps/ascend-ocr/src/api/rest/rest_endpoints.py) | `lang` is declared as a bare optional string with no `Form()` marker, and a search for `Form(` across `apps/ascend-ocr/src` returns zero matches anywhere in the module. FastAPI classifies an unannotated scalar as a query parameter regardless of the `UploadFile` beside it, so the multipart field every documented client sends is never read, `lang` stays unset, and line 35 falls through to `DEFAULT_LANGUAGE`. Every REST request runs the English model whatever it asks for. Proven behaviourally rather than by inspection: the same file posted twice, once with the language in the query string and once as a form field, produced `ascendocr_ocr_requests_total{language="pl",surface="rest"} 1.0`, the query one. The MCP surface is unaffected, proven the same way, because `ocr_process` binds `lang` from the JSON-RPC arguments object. [apps/ascend-ocr/AGENTS.md](../apps/ascend-ocr/AGENTS.md) documents the endpoint as multipart with an optional `lang`, so code and contract disagree and one of them must move | Already active, and has been since the parameter was written. Note before fixing: [AscendOcrClient.java](../apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/ingestion/client/AscendOcrClient.java) was changed on 2026-09-07 to send `lang` in the query string, which is correct against the server as it stands and is currently the only caller in the stack that selects a language successfully. Fixing the server to read a form field breaks that client unless both move in the same commit |
| A23 | The Polish OCR e2e spec records A22's bug as intended behaviour | [apps/ascend-ocr/e2e/testing/3-ocr-polish-test.md:7,64](../apps/ascend-ocr/e2e/testing/3-ocr-polish-test.md) | Both lines state that the service returns its configured `DEFAULT_LANGUAGE` regardless of the per-request `lang`, phrased as documented design rather than as a defect. Somebody observed the symptom and wrote it into an immutable spec as the expectation. That is why twelve OCR specs never caught A22: the one spec positioned to catch it was taught not to. The spec still passes, because its real proof is a Polish-character assertion and the English model reads Latin script well enough to satisfy it | Already active. More damaging than A22 itself, since it is the mechanism that keeps A22 invisible. Fixing A22 without rewriting these two lines leaves the suite asserting the old broken behaviour |
| A24 | ascend-web-hunter's session establish never persists a session record | [apps/ascend-web-hunter/e2e/testing/10-session-establish-test.md](../apps/ascend-web-hunter/e2e/testing/10-session-establish-test.md), capture path behind the session establish endpoint on port 7021 | The call itself is correct: HTTP 200, `status="login_required"`, right target, non-empty `vnc_url`, and the container logs show the monitor starting with a real headful Chromium. Nothing is ever written. The Redis key read as absent continuously for thirteen minutes across two independent pollers, the session status endpoint reported `"status":"none"` throughout, and the monitor browser then exited on its own timeout rather than after a capture, dropping the container from two browser trees to the one the pool holds permanently. Not timing variance, since the full ten-minute budget elapsed. Not a crash, since it shut down cleanly. Not test contention, since nothing else was running. The spec's own "Two behaviours this spec found live" section documents the opposite, recorded from a live run against port 7021, so this is a regression against observed behaviour rather than a wrong expectation | Already active. The e2e verdict is FAIL. Anyone relying on an established session being reusable gets no session and no error saying so |
| A25 | The OCR container's memory ceiling is below the worst input its own guard admits | [compose.yaml:95-98](../compose.yaml), estimate from [apps/ascend-ocr/src/config/startup_banner.py:26-50](../apps/ascend-ocr/src/config/startup_banner.py) | The service already warns about this at every boot and has been doing so unheeded. Reproduced from the code and the live settings: 635 base plus 1001 language cache plus 11759 detection plus 795 other plus 12 retained equals 14201 MiB against a 12288 MiB cap. Detection is 83 percent of it and is bounded by `OCR_DETECTOR_MAX_SIDE` at 1536, which is 2.36 megapixels at roughly 4984 MiB per megapixel. The cache term assumes eight resident languages because `ENGINE_CACHE_MAX_SIZE` is 8 in compose while the image ships only English and Polish; correcting that to a realistic 143 still leaves 13343, above the cap. A document at the largest size the pixel guard accepts is admitted at the door and then the container is killed mid-document | Already active but unexercised. Every spec in the 2026-09-07 sweep ran far below the ceiling, so nothing caught it, and it will not surface until a real large scan arrives. Three closures, all needing an owner decision: raise the container to 15G, which fits the 23.47 GiB host but eats headroom the memory budget allocates elsewhere; lower `OCR_DETECTOR_MAX_SIDE` to about 1450, which fits inside 12G but reverses the owner's measured choice recorded in ADR-006; or lower `ENGINE_CACHE_MAX_SIZE` from 8 to 2 to match the shipped languages, which frees 858 MiB, is free and obviously correct, and is insufficient alone |
| A26 | One OCR e2e spec fails on a wall-clock bar that measures the test harness, not the service | [apps/ascend-ocr/e2e/testing/1-invalid-input-test.md:52](../apps/ascend-ocr/e2e/testing/1-invalid-input-test.md) | Lists "per-call duration under 500 ms" under Expected as a hard gate. Measured on an idle host: the Bruno runner reports 333 to 359 ms for the same request a raw curl completes in 208 to 221, so roughly 130 ms of every Bruno number is Node startup and multipart encoding. Under the concurrent sweep the gap widened to 965 against 226 on one weather call, about 740 ms of harness and contention. The 2026-09-07 sweep failed this spec at 519 ms with every behavioural assertion passing. [apps/ascend-weather-mcp/e2e/testing/1-invalid-input-test.md:89](../apps/ascend-weather-mcp/e2e/testing/1-invalid-input-test.md) carries the identical measurement but phrases it as a diagnostic hint and states outright that it is not an assert, so two runners produced opposite verdicts from the same evidence. These two are the only specs of all 51 that carry a timing bar | Already active. Fails on a loaded host regardless of product health. Closing it means matching the OCR wording to the weather wording, since what it is really trying to prove, that validation short-circuits before the engine, is already proven far more robustly by the 422 arriving at all |
| A27 | Two OCR specs assert facts nothing on this stack can observe | [apps/ascend-ocr/e2e/testing/8-mcp-ssrf-rejection-test.md](../apps/ascend-ocr/e2e/testing/8-mcp-ssrf-rejection-test.md), [apps/ascend-ocr/e2e/testing/11-mcp-file-uri-jail-test.md](../apps/ascend-ocr/e2e/testing/11-mcp-file-uri-jail-test.md) | Spec 8 asserts no outbound call was made to the cloud metadata address, but neither the host nor the container carries a packet-filtering tool to watch with. Spec 11 asserts the service never opened the system password file, verifiable through a process audit that is not configured and whose tools are absent from the container. Both runners in the 2026-09-07 sweep reported these as unverifiable rather than ticking them, which is correct behaviour and is what surfaced the problem | Already active. Harmless while runners stay honest, dangerous the moment one ticks a box it cannot check. The security behaviour is genuinely proven by the `UNSAFE_URI` rejection arriving, so the closure is to delete the unobservable line rather than install audit tooling |
| A28 | Every e2e spec in the repository is written PowerShell-only | All six `apps/*/e2e/testing/*-test.md` suites, in prerequisite `Test-Path` checks, `ConvertFrom-Json` parsing and `$env:TEMP` output paths | Contradicts the repository's own documentation standard, which asks for a PowerShell and a Unix-shell version of every command. Automated runners get a POSIX shell, so in the 2026-09-07 sweep every runner improvised an equivalent and said so in its notes: `ls` for `Test-Path`, a Python one-liner for `ConvertFrom-Json`, the scratchpad path for `$env:TEMP`. It worked every time, but it means each run executed something slightly different from what the immutable spec says, which is most of the point of having one. One runner also wasted a 75-second engine call by passing a PowerShell-style output path to bash and having to repeat it | Already active on every run that is not driven from a PowerShell prompt, including any hosted runner. Costs correctness of the audit trail rather than correctness of the result |
| A29 | The agent's MCP tool discovery raises on a stale session instead of reconnecting | [apps/ascend-agent](../apps/ascend-agent), tool discovery path through Spring AI's sync MCP tool callback provider and its client's tool listing call | After roughly four hours idle, every registered MCP client held a session its server no longer recognised. Tool discovery threw a transport session-not-found exception, surfacing to the caller as HTTP 500 with no useful message. Reproduced three times, each attempt implicating a different server, ascend-web-hunter then ascend-audio-scribe then ascend-weather-mcp, and repairing exactly one session while still failing the request that triggered the repair. A fourth request then succeeded in 8.65 seconds with a correct grounded answer. So a cold stack costs one failed request per registered MCP server before it works, three in a row on this stack. Confirmed as idle-related rather than fast-cycling: a clean re-run after warm-up passed with no 500 and no repair. Ruled out as a credential problem in the same runs, since the dead OpenAI key is caught inside the retrieval and semantic-memory clients, logged as a warning, and the orchestration continues without that context rather than aborting | Already active on any stack left idle. Indistinguishable from a flaky service to a user, and the first e2e spec of the sweep failed on it before anything else could be measured |
| A30 | ascend-web-hunter's concurrency table omits a conflict it needs | [apps/ascend-web-hunter/e2e/README.md](../apps/ascend-web-hunter/e2e/README.md) "Parallelism and execution order" | Test 8 is listed as safe beside tests 1, 2, 4, 5 and 9, and its conflict with test 10 over the shared session key is declared. Its conflict with test 3 is not, even though test 3 reads `example.com` and test 8 asserts that clearing `example.com` finds zero cached entries. The table is incomplete in a way that lets a careful reader schedule the collision rather than merely omitting an obvious one. Separately, test 8's final "no collateral damage" assertion compares the whole session key set against a baseline, which is only meaningful in an isolated run, and the spec does not say so. Both were hit in the 2026-09-07 sweep: test 8 failed on both assertions, and a re-run in isolation passed every one | Already active. Produces failures that look like product defects and are not, which is exactly the environmental noise the suite README warns about |
| A31 | Three files tell you to run the agent on the host with Gradle; it runs as a container | [AGENTS.md:215](../AGENTS.md), [apps/ascend-agent/AGENTS.md:21](../apps/ascend-agent/AGENTS.md), [apps/ascend-agent/e2e/README.md:154](../apps/ascend-agent/e2e/README.md) | All three give a Gradle bootRun command as the way the service runs, and the e2e README lists it as a prerequisite before any spec. On this stack `ascend-ai-agent` is a compose service and has been running as a healthy container throughout. Both routes work, but a reader following the prerequisite literally starts a second instance competing for port 9917 | Already active. Also relevant to A25 and the memory budget, since a containerised agent is subject to a compose memory limit the host-run instructions never mention |
| A32 | ascend-memory documents three environment variables its code never reads | [apps/ascend-memory/AGENTS.md:56-60](../apps/ascend-memory/AGENTS.md) | Documents `MEM0_COLLECTION_NAME` with a default of `ascend_memory`, plus `MEM0_EMBEDDING_MODEL` and `MEM0_EMBEDDING_DIMS`. A recursive search across `apps/ascend-memory/src` returns zero references to any of the three. The collection is chosen per provider from a hardcoded configuration table at [apps/ascend-memory/src/config/config.py:29,37,45](../apps/ascend-memory/src/config/config.py), giving `ascend_memory_768` or `ascend_memory_1536`, and the live service uses `ascend_memory_1536`. Qdrant currently holds `ascend_memory`, `ascend_memory_768` and `ascend_memory_1536` side by side, so the documented name exists as an orphan from before the dimension suffix | Already active. An operator setting `MEM0_COLLECTION_NAME` to redirect the service gets no error and no effect, and anyone inspecting `ascend_memory` in Qdrant reads an empty historical collection while the live data sits in another |
| A33 | The OCR contract-test fixtures have no reproducible capture procedure, and the one that exists does not reproduce them | [apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/service/ingestion/client/AscendOcrClientLiveContractTest.java:30-34](../apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/service/ingestion/client/AscendOcrClientLiveContractTest.java), fixtures at `src/test/resources/ascend-ocr/openapi-contract.json` and `real-ocr-response.json` | Two fixtures are captured by hand from a live ascend-ocr instance on port 7022, one from FastAPI's generated schema and one from a real upload. The entire procedure is a block comment above two constants in the test. A search of the whole repository for both filenames and both constant names finds no script, no build target, no continuous-integration step and no documentation, so regenerating them correctly depends on somebody finding and reading that one comment. Worse, the two commands as written do not reproduce the committed bytes: `openapi-contract.json` is LF throughout while `real-ocr-response.json` is CRLF throughout, with no `.gitattributes` rule for either path and this repository's `core.autocrlf` set to `input`, which normalises CRLF to LF on commit. So the CRLF file was committed under different settings or written by a shell that re-encodes redirected output, and a future regeneration on any Unix-like shell will produce a diff that is entirely line endings | Already active. Bites the next person who has to refresh either fixture, which is now, since the OCR language fix changes what the schema says |
| A34 | Half of the OCR contract test no longer checks the contract | [apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/service/ingestion/client/AscendOcrClientLiveContractTest.java](../apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/service/ingestion/client/AscendOcrClientLiveContractTest.java) | The `file` half is a real contract check: the required field name is read out of the checked-in schema fixture and asserted against the request the client actually builds, so client drift fails the test. The `lang` half is not. It asserts a string literal that was chosen to match the client's own constant, because the fixture still describes the pre-fix query-parameter shape and the new location cannot be read from it. It would still catch a regression back to the query string or the field vanishing, which is real value, but it cannot detect a disagreement with the service, which is what a contract test is for. Introduced today by the language fix, which had to change the assertion before the fixture could be refreshed | Already active until the fixture is regenerated against a rebuilt ascend-ocr and the assertion is driven from it the way the `file` half already is. Closing A33 is a prerequisite for closing this |
| A35 | A session record with an empty cookie jar is reported as a live authenticated session | [apps/ascend-web-hunter/src/session/session_manager.py](../apps/ascend-web-hunter/src/session/session_manager.py) `status()`, [apps/ascend-web-hunter/src/reader/web_reader.py](../apps/ascend-web-hunter/src/reader/web_reader.py) `_has_stored_session`, both reading [apps/ascend-web-hunter/src/reader/cloudflare/cookie_manager.py](../apps/ascend-web-hunter/src/reader/cloudflare/cookie_manager.py) | Three readers of the same record disagree, and the one that is right proves the check costs nothing. `validate()` requires the cookie list to be non-empty and returns false with a log line when it is not. `status()`, behind the session status REST and MCP endpoints, asks only for the remaining time to live, and `get_auth_ttl_remaining` falls back to `SESSION_AUTH_TTL_SECONDS` minus the record's age whenever no cookie carries a hard expiry, which an empty jar never does. That default is 1209600 seconds, exactly fourteen days, so a freshly written empty record reports itself active with a fortnight remaining for a domain nobody ever logged into. `_has_stored_session` is `get_storage_state(...) is not None`, and that returns a merged state as soon as an entry exists with positive time to live whatever is inside it, so `_prefer_browser` routes on it and one empty capture silently forces every later read of that domain and profile onto the heavier browser tiers, paying exactly the cost the tiering exists to avoid for no authentication benefit. Verified directly against the source rather than taken from a report | Already active, and the same class of bug as the one commit `02bacdb` was trying to fix from the other direction. That attempt put the check on the write side, which broke the documented contract and produced A24. This one belongs on the read side, where `validate()` already does it. Found while assessing whether A24's fix should write a record for a page nobody was challenged on: it should, and this is why the record was thought harmful |
| A56 | Web-hunter spec 7 Part 3 targeted a page whose challenge the NoVNC monitor can never declare cleared on its own address, and asserted a cookie that appears without any human action | [apps/ascend-web-hunter/e2e/testing/7-authenticated-realworld-scraping-test.md](../apps/ascend-web-hunter/e2e/testing/7-authenticated-realworld-scraping-test.md) Part 3, [apps/ascend-web-hunter/e2e/testing/templates/7-authenticated-realworld-scraping-tasks.template.md](../apps/ascend-web-hunter/e2e/testing/templates/7-authenticated-realworld-scraping-tasks.template.md), [docs/api/request/AscendAI/web-hunter/testing/captcha-clearance-blocked.yml](api/request/AscendAI/web-hunter/testing/captcha-clearance-blocked.yml) | The target was `https://www.google.com/recaptcha/api2/demo`. The monitor's captcha branch (`_poll_captcha` in [apps/ascend-web-hunter/src/reader/strategies/novnc_strategy.py](../apps/ascend-web-hunter/src/reader/strategies/novnc_strategy.py), lines 105 to 131) captures a session only once `ChallengeDetector.is_content_accepted` is true for the page it watches, and that page keeps its `recaptcha/api.js` script, a block signature in `apps/ascend-web-hunter/src/reader/cloudflare/challenge_dictionary.json`, in the DOM after the solve, so by code reading the monitor can never clear on that address, and the pass recorded in the 2026-09-03 run must have come from another page opened in the same window. The spec's claim that `_GRECAPTCHA` appears only on interaction was measured false on 2026-09-10, when it appeared on a bare load of a Google demo page, so the capture check proved nothing about a human. On 2026-09-10 a monitor on that address ran the full 600 s and logged `outcome=rejected` | Fix prepared in the working tree, uncommitted: Part 3, its template and the Bruno request target `https://democaptcha.com/demo-form-eng/hcaptcha.html`. Its `hcaptcha.com/1/api.js` script escalates every tier (26 s from the request to the monitor on 2026-09-10), and the form's Submit navigates to a page without the widget, which the monitor declared cleared at 09:48:56Z and captured under `session:democaptcha.com:default` with auth cookies `evod95wg4`, `hmt_id` and `__cflb` and the WAF cookie `__cf_bm`. The capture check asserts `hmt_id`, which hCaptcha sets on the checkbox click, so it proves a human acted and not that the image task was solved, and the monitor also clears on an unsolved Submit because the failure page has enough words and no script. A research pass found no public page that both escalates and sets a solve-only cookie, so the spec states that trade. The retargeted Bruno request answered HTTP 428 with `status="human_intervention_required"` and a `vnc_url` in 14.4 s against the live stack on 2026-09-10 at 09:55Z, three of three tests passing. Since 2026-09-10 the human part is spec 11 ([apps/ascend-web-hunter/e2e/testing/11-captcha-solve-and-reuse-test.md](../apps/ascend-web-hunter/e2e/testing/11-captcha-solve-and-reuse-test.md)), which asserts reuse: after the solve a second read of the same page must answer HTTP 200 with `status="success"`, a second 428 being a FAIL to register as a product defect, while spec 7 keeps only its automated parts |
| A58 | The scraping compose file could not start on its own: its own project name collided with the containers the main file's `include:` had already created | [compose.ascend-web-hunter.yaml](../compose.ascend-web-hunter.yaml) `name:` key and the `container_name` values on `searxng`, `flaresolverr`, `ascend-web-hunter` and `ngrok-ascend-web-hunter`; the `REDIS_URL` default on the `ascend-web-hunter` service; `.run/compose ascend-web-hunter [rebuild-recreate].run.xml` (`docker compose -f compose.ascend-web-hunter.yaml up -d --build --force-recreate`) | The file declared `name: ascend-scrapper`, so starting it alone formed a second Compose project instead of joining `ascend-ai`, the project `compose.yaml`'s `include:` merges it into when both run together. Starting it alone after the main stack (project `ascend-ai`) and the external prerequisites (project `local-dev`) had been stopped failed with `Error response from daemon: Conflict. The container name "/searxng" is already in use by container "ba502bbca40e..."`, because the file's four fixed `container_name` values already existed under the running `ascend-ai` project. Verified with `docker compose config` on the main file: the `include:` ignores the sub-file's own `name:` and always resolves to project `ascend-ai`, so the sub-file's `name:` only ever mattered when it ran alone. Second gap found the same way: `REDIS_URL` defaulted to `redis://host.docker.internal:6379/0`, the `local-dev` project's external Redis, which is off when the scraping stack runs alone, so the session store degraded to memory and e2e specs 7 through 10 failed their Redis assertions | Already active whenever the scraping file is started alone, as the owner did on 2026-09-10 via the IDE run configuration after stopping the main stack and `local-dev`. The profile-gated bundled `redis` service in `compose.ascend-web-hunter.yaml` stays as designed: off by default behind the `redis` profile, `container_name` `ascend-scrapper-redis`, network alias `redis`, no `depends_on` from `ascend-web-hunter`, and `REDIS_URL` defaulting to `redis://host.docker.internal:6379/0`. Running the file alone on a machine without a host Redis needs the two `.env` lines `COMPOSE_PROFILES=redis` and `REDIS_URL=redis://redis:6379/0`, which `.env.example` now documents as commented-out examples, and the end-to-end suite README says the `docker exec redis` commands in the specs then target `ascend-scrapper-redis`. The project name stays `ascend-scrapper` by the owner's decision: the scraping stack is its own project and must not merge with `ascend-ai`. The container-name collision between the two projects is resolved by `docker compose down` on the other project before switching, one stack at a time by the owner's decision, since stopping its containers only leaves the names taken |
| A59 | The text reader drops every item title on listing pages: trafilatura's default precision pass keeps the prices and stock lines and nothing else | [apps/ascend-web-hunter/src/reader/extraction.py](../apps/ascend-web-hunter/src/reader/extraction.py) `extract_text_with_fallback`, the function `WebReader._execute_strategy` and `TrafilaturaStrategy.extract` return text-mode `content` from, and [apps/ascend-web-hunter/src/config/config.py](../apps/ascend-web-hunter/src/config/config.py) `READABILITY_FALLBACK_MIN_CHARS` | Measured on 2026-09-10 on `https://books.toscrape.com/`: `trafilatura.extract(html)` returned 319 characters of prices and `In stock` lines and no book title, `trafilatura.extract(html, favor_recall=True)` returned 1143 characters with the titles, and plain BeautifulSoup `get_text` after `remove_noise_tags` returned 1851 characters. The readability-lxml fallback of ADR-007 never ran because 319 is above the 200 character floor, and its own output on that page (568 characters of HTML) has no titles either. The same shape measured at 175 and 319 characters on two more books.toscrape.com listings and on quotes.toscrape.com page 2, where the default pass dropped 29 lines of quotes that recall mode kept. Nine article pages measured the same day were complete on the default pass at 0.90 to 1.00 of their plain text | Already active on every text-mode read of a catalogue or listing page. Fix prepared in the working tree: `extract_text_with_fallback` runs a second pass with `favor_recall=True` when the first is shorter than `CONTENT_RECALL_FALLBACK_RATIO` (default 0.75, derived in `apps/ascend-web-hunter/docs/architecture/decisions/ADR-009-recall-pass-for-thin-precision-extractions.md`) of the page's plain text and returns the longer result, with saved copies of the books listing and a gnu.org article under `apps/ascend-web-hunter/tests/fixtures/` as regression tests |

### Currently dormant

| # | Defect | Where | Effect | What would make it bite |
| :- | :--- | :--- | :--- | :--- |
| A7 | `.pre-commit-config.yaml` scoped every hook to `^PaddleOCR/` | [.pre-commit-config.yaml:7,9,15](../.pre-commit-config.yaml) | Ruff, ruff-format and mypy currently only ever run against ascend-ocr; no other Python module has a pre-commit lint or type gate at all, which is itself worth knowing, but the specific defect here is narrower | `AscendWebSearch/` and `AudioScribe/` already lived through this trigger this session. PaddleOCR's own rename to `apps/ascend-ocr/` retargets the filter to `^apps/ascend-ocr/` in the same change that moves the directory ([.pre-commit-config.yaml:7,9,15](../.pre-commit-config.yaml)), so this third instance of the pattern never actually bites, pending that change landing as a commit |
| A9 | ascend-weather-mcp and ascend-memory sit at 73 and 58 percent of their own container memory caps | [docs/architecture/memory-budget.md](architecture/memory-budget.md) "Limits that look wrong, and the evidence" (ascend-weather-mcp 512 MiB limit, ascend-memory 512 MiB limit) | Both are JVM or proxy services whose peak is drifting upward on every fresh counter reading, and both would be killed by their own limit long before the host noticed anything | Safe today. Bites the next time either service's peak drifts past its own ceiling under sustained load; they are named as the two most likely next container-limit kills in the stack |
| A46 | Observation, dormant: docling-serve's respawned worker idles at 20 to 25 percent CPU | [compose.yaml](../compose.yaml) `docling-serve` | After docling-serve respawned its worker (see A38), `docker top` showed pid 61157 at 19.5 percent CPU over 15 minutes with no request in flight | Dormant. Third-party image, cause not found, no fix prepared. Recorded so the next reading of `docker stats` has a baseline |
| A47 | Observation, dormant: the OCR engine runs single-threaded | [compose.yaml](../compose.yaml) `ascend-ocr`, PaddleOCR engine construction in `apps/ascend-ocr/src` | `docker stats` showed 104 percent CPU against a 4 core limit during inference, so inference speed is one core's speed | Dormant. Candidate improvement is a `cpu_threads` engine argument, not done, needs a measured before and after. No fix prepared |
| A57 | Observation, dormant: the `session:google.com:default` record found next to a NoVNC monitor that reported no session written was written by the FlareSolverr tier, not by the monitor | [apps/ascend-web-hunter/src/reader/strategies/flaresolverr_strategy.py](../apps/ascend-web-hunter/src/reader/strategies/flaresolverr_strategy.py) lines 70 to 82, [apps/ascend-web-hunter/src/reader/cloudflare/cookie_manager.py](../apps/ascend-web-hunter/src/reader/cloudflare/cookie_manager.py) `save_flat_cookies` (line 303) and `save_storage_state` (line 234), [apps/ascend-web-hunter/src/reader/strategies/novnc_strategy.py](../apps/ascend-web-hunter/src/reader/strategies/novnc_strategy.py) `_poll_captcha` (lines 105 to 131) and the timeout branch (lines 219 to 230) | On 2026-09-10 the monitor for `https://www.google.com/recaptcha/api2/demo` logged `timed out after 600s (outcome=rejected)` and `no session was written` at 09:13:46Z while Redis held `session:google.com:default` saved at 09:04:05Z with one cookie, `__Secure-ENID`. The log line is right. The container log with timestamps shows `[CookieManager] Saved session to Redis for google.com` at 09:04:05.633Z, one millisecond before `FlareSolverrStrategy: WAF/Cloudflare block detected`, and the monitor did not start until 09:04:24Z. `FlareSolverrStrategy.get_html` persists every non-empty cookie set FlareSolverr's browser received through `save_flat_cookies` (line 74) before it runs `is_login_required` and `is_blocked` (lines 76 to 82), and the comment at lines 70 to 73 says the write is unconditional on purpose so a login site's auth cookies are not discarded. The monitor's captcha branch writes only inside `_poll_captcha`, once `is_content_accepted` is true or a `cf_clearance` cookie is present, and then logs `captcha solved` and stops, so a monitor that reaches the timeout branch has written nothing. No code path writes a record and then reports that none was written | Dormant as a defect, recorded because the same FlareSolverr write leaves a record holding only a consent or preference cookie under a domain the same tier then declared blocked. `WebReader._has_stored_session` treats such a record as a stored session and routes later reads of that domain onto the browser tiers, the A35 shape with a non-empty jar. It happens whenever a challenged page hands FlareSolverr a cookie before blocking, which Google and democaptcha both did on 2026-09-10. No fix prepared |

### Unverifiable in this session

| # | Defect | Status |
| :- | :--- | :--- |
| A10 | Sixteen OpenSpec validations failing on placeholder sections, one change missing its deltas | Could not run `openspec validate` from this session (no shell tool available). A text search for placeholder markers (`TBD`, `TODO`, `[PLACEHOLDER]`, `{{...}}`) across `openspec/changes/` found two hits, [openspec/changes/add-web-search-scraping-e2e/test-spec.md](../openspec/changes/add-web-search-scraping-e2e/test-spec.md) and one archived design doc that would not count toward an active-change validation count. That is not enough to confirm or refute "sixteen." Whoever next has shell access should run `openspec validate --all` (or the project's equivalent) and update this row with the real count and the specific change missing its deltas |

---

## Declined

| # | Defect | Status |
| :- | :--- | :--- |
| D1 | Several databases are reachable on every network interface with no password | Declined by the owner for his own machine. Per [ADR-M003](architecture/decisions/ADR-M003-external-infrastructure-prerequisites.md), PostgreSQL, Redis, Qdrant and the object store are deliberately external prerequisites this repository does not configure, so there is no compose file or settings file this register can point at as the source; the bind address and auth posture live entirely in the operator's own local setup, which is exactly why this is an accepted risk rather than a repository defect |

---

## Fixed

Grouped by area. Each row names the current evidence in the running repository; where a specific commit could be
matched from `.git/logs/HEAD`, it is given. Several of these predate the reflog window this session could read
(entries below note that explicitly rather than inventing a SHA).

### ascend-ai-agent and cross-service

| # | Defect | Evidence | Commit |
| :- | :--- | :--- | :--- |
| F1 | Embedding provider configuration key never bound, so requests omitting it failed | [apps/ascend-memory/src/config/config.py:25-116](../apps/ascend-memory/src/config/config.py), `PROVIDER_CONFIGS` plus `MEM0_DEFAULT_PROVIDER` with a bound default (`lmstudio`); a request omitting `provider` now resolves through `provider_config()` correctly | Not identifiable in the available `.git/logs/HEAD` window; current code is unambiguously correct |
| F2 | Testcontainers restarted per test class instead of once per run | [apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/integration/TestcontainersBase.java:29-42](../apps/ascend-agent/src/test/java/com/lukk/ascend/ai/agent/integration/TestcontainersBase.java), a static initializer with an explicit Javadoc explaining the original bug (the `@Container` annotation tearing down the first subclass's containers while Spring's context cache kept reusing stale ports) and the fix (a singleton static block, no `@Container`) | Not identifiable in the available window |
| F3 | The document converter's workers died without retry | [apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/ingestion/client/DoclingClient.java:97-114](../apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/ingestion/client/DoclingClient.java), `postWithRetry` retries a transient `ResourceAccessException` up to `MAX_ATTEMPTS = 3` with a 500 ms backoff before giving up | Not identifiable in the available window |
| F4 | A client sent its file under the wrong multipart field, so every OCR call returned 422 | ascend-ai-agent's ascend-ocr REST client now sends the field ascend-ocr's own endpoint expects | `fix(agent): send the field the OCR service actually asks for` (`814a1b33f0ec816177009ed4c4e02e83a9edd850`) |
| F5 | The same client sent the language in the body while the service reads it from the query | Same fix as F4, same commit | `814a1b33f0ec816177009ed4c4e02e83a9edd850` |
| F6 | A container metrics exporter was blind on this host while scraping green | [infra/observability/README.md:45](../infra/observability/README.md), the exporter now reads the Docker Engine API stats endpoint directly over the Docker socket rather than a host-filesystem cgroup path that did not resolve on this host | `feat(observability): bound three services by measurement and see it happen` (`264ebcc0a8b84fbfb5a73c990f4cdf7102afa874`) |
| F7 | Around fifty tests silently reached the internet | Not independently confirmed. No `pytest-socket`, `disable_socket`, or equivalent network-blocking fixture was found in any module's `pyproject.toml` or `conftest.py` in this session. Recorded as reported-fixed; whoever can diff the actual test suite against its prior state should confirm what mechanism, if any, now blocks outbound calls |
| F8 | An unnecessary type suppression failed the type check | Partial match only. `warn_unused_ignores = true` is set in four Python modules' `pyproject.toml` (`ascend-audio-scribe`, `ascend-web-hunter`, `ascend-ocr`, `AscendMemory`), which is the mechanism that would surface this class of defect, but that setting predates this session. The closest concrete instance found is [openspec/changes/stop-ocr-getting-stuck-on-large-jobs/tasks.md:292-297](../openspec/changes/stop-ocr-getting-stuck-on-large-jobs/tasks.md), which removed two unused ruff `noqa: N802` directives, a ruff suppression rather than a mypy `type: ignore`. Recorded with that caveat rather than a confident match | `5bb86191855cb1173ff8c3b4031958acc7c0af0b` (ruff cleanup only; mypy instance not located) |
| F9 | A coverage gate was declared but never enforced in two modules, leaving one at 98.98 percent | [.github/workflows/ci.yaml:142-155](../.github/workflows/ci.yaml), the comment names both: `ascend-web-hunter` and `ascend-memory` declare `fail_under = 100` in `[tool.coverage.report]` but do not bake it into pytest addopts, so a bare `pytest` used to pass without enforcing it; CI now runs `pytest --cov=src --cov-branch --cov-report=term-missing --cov-fail-under=100` explicitly for both | `test: run the coverage gate two modules declared but never enforced` (`d92c2b6e59b19e5a7c379a985c2a9ee0adcd2600` to `e3472d9f0e1bf1c675ee2bb79eef6dab02bd8885`) |
| F10 | Three continuous integration references pointed at request files that had moved | [.github/workflows/e2e.yaml](../.github/workflows/e2e.yaml) now consistently uses `web-hunter/testing/...`, `ocr/...`, `memory/testing/...` and `weather-mcp/...` under [docs/api/request/AscendAI/](../docs/api/request/AscendAI/), matching the current Bruno collection layout | `refactor: rename the web service to ascend-web-hunter, everywhere` (`d89fb420e727a144ecdf6aa83a814c12c25982b1`) and `docs(api): give every endpoint a request, in one place per module` (`d4d0de3039a41852d491c7c1e1c7460d63c1b991`) |
| F11 | Documentation across two modules described thread-pool execution years after it moved to a separate process | Not confirmed. A search for stale `ThreadPoolExecutor` language in ascend-ocr and `ascend-web-hunter` docs found only current, accurate references (ascend-ocr's own arc42 constraints page correctly describes its `ProcessPoolExecutor`). Could not identify the two modules this entry refers to; recorded as unconfirmed rather than mapped to evidence that does not fit |
| F12 | A compatibility patch was dead against its own pinned dependency | Weak match. The closest candidate is AscendMemory's [ADR-006](../apps/ascend-memory/docs/architecture/decisions/ADR-006-mem0ai-2x-upgrade.md), which removes an `OpenAILLM.generate_response` monkey-patch, but it is dated 2026-06-01 (roughly three months before this register's window) and describes the patch being retired by a deliberate mem0ai 1.0.3 to 2.0.4 upgrade, not found silently dead while still pinned. See the verification notes above; this entry may be misattributed | `ADR-006`, dated 2026-06-01; no matching recent commit found |
| F13 | Stale specification counts in the cost document | [docs/E2E_COST.md](../docs/E2E_COST.md) now states "as they stood on 2026-09-04" and reconciles every per-module spec count against the current suite | `docs(cost): price the remaining gaps and correct the sweep totals` (`4cd292a0a97a3ab09b3d82368105727a36fe68b0`) |
| F14 | An observability document claimed six containers and listed five | [infra/observability/README.md:91-93](../infra/observability/README.md) now states the reconciliation directly: five of the six application containers get a `service` log label, `ascend-weather-mcp` is deliberately console-silent per its own `application.yml`, and the doc says so instead of leaving the discrepancy unexplained | Not identifiable to a single commit in the available window; consistent with `264ebcc0a8b84fbfb5a73c990f4cdf7102afa874` |
| F32 | Release workflow read each app's previous version via `git show <prev-tag>:<manifest-path>`, using today's path, so a module rename broke the bump guard at the next release rather than at the rename (formerly A8) | The git-tag lookup was not made path-move-safe. It was removed. [.github/workflows/release.yaml](../.github/workflows/release.yaml) no longer reads any previous tag: `prepare` now reads each app's current version straight from the topmost `## [x.y.z]` entry in that app's own `apps/<app>/CHANGELOG.md` (`read_changelog_version`, lines 121-124), and `build-and-push` guards against re-publishing an already-shipped version by asking Docker Hub and GHCR directly whether `<image>:v<version>` already exists (`docker manifest inspect`, lines 194-246), not by diffing git history. `ci.yaml` gained a companion `verify-changelog` job (lines 88-202) that fails a pull request when a changed app's `CHANGELOG.md` top entry was not bumped past its base-ref version. [.github/workflows/README.md](../.github/workflows/README.md) "The bump guard" section states the intent directly: "The guard no longer looks at git history at all, which is what makes it immune to a module's path moving between releases (the previous git-tag-based guard was retired for exactly this reason, see the defect register)." No `ascend-ai_*` tag has been cut yet, so the old mechanism was never exercised in anger before its removal | Not committed as of this check. Fill in the commit hash here once this change lands on `master` |
| F35 | The docling per-page retry caught `ResourceAccessException` only, and a connection the server dropped mid-request did not raise one (formerly A38) | [apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/ingestion/client/DoclingClient.java](../apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/ingestion/client/DoclingClient.java). docling-serve logged `Child process [10] died` with cgroup `oom_kill 0` and a 6.9 GB peak under a 10 GB limit. The client already retried twice on `ResourceAccessException`, but the lost connection surfaced during response extraction as a plain `RestClientException` (`Error while extracting response for type [java.lang.String] and content type [application/octet-stream]`) with a `java.net.SocketException` (`Unexpected end of file from server`) underneath, which the catch did not match, so the page was not retried and the whole ingestion became HTTP 422, while two manual retries of the same call returned 200. The catch now walks the cause chain for an `IOException`, and the retry count and delay are the configuration properties `app.docling.retry-attempts` and `app.docling.retry-delay`, defaulting to two retries and 500 ms | `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F38 | agent spec 4 required one Qdrant point holding both seeded facts, mem0 stores one atomic fact per point (formerly A41) | [apps/ascend-agent/e2e/testing/4-semantic-memory-test.md](../apps/ascend-agent/e2e/testing/4-semantic-memory-test.md), its template. Observed two points for `frostySemanticMemoryTest`, one carrying `Luke` and one `software engineer`, while the recall step answered correctly. The assertion failed a correct system. It is now across all points returned for the user | `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F39 | Four suites' MCP specs called the session id UUID-formatted (formerly A42) | audio-scribe, memory, ocr and web-hunter `e2e/testing/` specs, templates and READMEs. Measured on 2026-09-09 with an `initialize` handshake against ports 7017, 7020, 7021 and 7022: each value is 32 hexadecimal characters without hyphens, for example `9b340135f8b44aec91dc3a5d7d53aa79`. Only ascend-weather-mcp on 9998, Spring AI rather than FastMCP, emits a hyphenated UUID, so its specs were correct as written. The wording is changed in the four FastMCP suites, weather-mcp untouched | `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`), `docs(e2e): correct the weather and memory specs, and align their versions` (`5e5bd3e898e7c262bd691a89b45b49ecfb75f8c2`), `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`) and `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`) |
| F40 | Eleven Bruno request scripts chained an invalid Chai `.or.` property, and one asserted a property on a bare array (formerly A43) | [docs/api/request/AscendAI](api/request/AscendAI): `ascend-agent/prompt.yml`, `mcp/memory/delete-memory.yml`, `mcp/memory/search-memory.yml`, `mcp/memory/wipe-memory.yml`, `transcribe/huggingface.yml`, `transcribe/local.yml`, `transcribe/openai.yml`, `web-hunter/testing/extract-example-com.yml`, `web-hunter/testing/mcp-list-tools.yml`, `web-hunter/testing/mcp-search.yml`, `web-hunter/testing/search-stable-query.yml`. Chai has no `or` property, so `expect(x).to.have.property("a").or.have.property("b")` throws `Invalid Chai property: or` and the test fails whatever the response holds. `search-stable-query.yml` additionally asserted `status` on a response that spec 2 defines as a bare JSON array. Each is rewritten as `expect(Object.keys(x)).to.include.oneOf([...])`, and the search request asserts the array shape spec 2 describes | `test(bruno): assert the real contracts across the collection` (`3819db68181386831ea7959b801944637b32631a`) |
| F41 | Four Bruno scripts asserted weaker or wrong conditions than their specs (formerly A44) | [docs/api/request/AscendAI](api/request/AscendAI): `ascend-agent/testing/memory-test-save.yml`, `ocr/testing/ocr-polish.yml`, `ocr/ready.yml`, `ascend-agent/testing/compaction-idempotency-prompt.yml`. `memory-test-save.yml` expected the body to be a string while Bruno's `getBody()` returns a parsed object for JSON, `ocr-polish.yml` only checked `language` is a non-empty string where spec 3 requires `pl`, `ready.yml` accepted an empty `version`, and `compaction-idempotency-prompt.yml` never checked the seeded facts spec 11 says the answer must reference. The first always failed, the other three passed on wrong output. Each now asserts what its spec expects | `test(bruno): assert the real contracts across the collection` (`3819db68181386831ea7959b801944637b32631a`) |
| F42 | Six spec text errors (formerly A45) | [apps/ascend-weather-mcp/e2e/testing/1-invalid-input-test.md](../apps/ascend-weather-mcp/e2e/testing/1-invalid-input-test.md), [apps/ascend-audio-scribe/e2e/testing/2-transcribe-openai-test.md](../apps/ascend-audio-scribe/e2e/testing/2-transcribe-openai-test.md), [apps/ascend-web-hunter/e2e/testing/6-tiered-scraping-test.md](../apps/ascend-web-hunter/e2e/testing/6-tiered-scraping-test.md), [apps/ascend-web-hunter/e2e/testing/8-session-clear-test.md](../apps/ascend-web-hunter/e2e/testing/8-session-clear-test.md), [apps/ascend-ocr/e2e/testing/10-mcp-credentials-rejection-test.md](../apps/ascend-ocr/e2e/testing/10-mcp-credentials-rejection-test.md), [apps/ascend-agent/e2e/testing/templates/9-prompt-cache-anthropic-tasks.template.md](../apps/ascend-agent/e2e/testing/templates/9-prompt-cache-anthropic-tasks.template.md). weather 1 claimed a steady-state latency under 50 ms where three raw curl calls measured 208 to 223 ms (Bruno's own durations 343 to 838 ms), audio 2 said to expect 200 from an egress probe that sends no Authorization header and always answers 401, web-hunter 6 used a sed `s` flag GNU sed does not have, web-hunter 8 gave a repository-relative `docker cp` path from the Bruno collection root, ocr 10 piped through `jq` without listing it as a prerequisite (not installed on this host, `jq: command not found`), and agent template 9 demanded a cold cache write where the spec prose accepts a cold write or a warm read. The same pass corrected the web-hunter spec 8 and 10 Concurrency sections, which still named `session:example.net:default` as the key both mutate while spec 10 writes `session:example.net:e2e-establish`, and the ocr deployment view sentence that claimed `MCP_ALLOWED_HOSTS` is unset in compose | `docs(e2e): correct the weather and memory specs, and align their versions` (`5e5bd3e898e7c262bd691a89b45b49ecfb75f8c2`), `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`), `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`), `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`) and `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F44 | Every module's version field lagged its changelog (formerly A49) | [apps/ascend-ocr/pyproject.toml](../apps/ascend-ocr/pyproject.toml) 0.1.0 versus changelog 0.2.1, `apps/ascend-web-hunter/pyproject.toml` 0.0.3 versus 0.0.5, `apps/ascend-memory/pyproject.toml` 0.1.0 versus 0.1.2, `apps/ascend-audio-scribe/pyproject.toml` 0.9.0 versus 0.9.2, `apps/ascend-agent/build.gradle.kts` 0.0.1 versus 0.1.1, `apps/ascend-weather-mcp/build.gradle.kts` 0.0.1 versus 0.0.3. The version each service reported (`SERVICE_VERSION` in ocr, the FastAPI application version in audio-scribe, the Version line in each AGENTS.md, the constraints documents) disagreed with the version the release workflow tags the image with. The four Python modules' pyproject, AGENTS.md, source and docs version lines match their changelogs, and the two Gradle files are bumped. ascend-ocr and ascend-audio-scribe no longer hardcode `SERVICE_VERSION`, they read it from the installed package's metadata at import time, and ascend-agent and ascend-weather-mcp advertise their MCP client/server version through the existing `application.yaml`/`application.yml` placeholder that resolves the Gradle build version, so each of the four modules has exactly one place its version lives | `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`), `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`), `docs(e2e): correct the weather and memory specs, and align their versions` (`5e5bd3e898e7c262bd691a89b45b49ecfb75f8c2`), `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`) and `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F45 | The PowerShell form of every JSON-body `curl.exe` block in the MCP specs failed on pwsh 7.6.5 (formerly A50) | sixteen spec files under `apps/*/e2e/testing/`: audio-scribe 4 and 5, memory 4 and 5, ocr 5, 6 and 8, weather-mcp 1 to 7, web-hunter 4 and 5. With `PSNativeCommandArgumentPassing=Windows`, the escaped body `-d "{\"jsonrpc\":...}"` reached curl.exe with its quotes stripped, curl printed `curl: (3) nested brace in position 101` and the initialize call to port 7021 answered HTTP 400, while the single-quoted body `-d '{"jsonrpc":...}'` answered HTTP 200 with an `Mcp-Session-Id` header, both measured through `pwsh -NoProfile -Command`. All sixteen blocks pass the body single-quoted with the JSON unchanged, and one converted initialize block per suite (ports 7017, 7020, 7021, 7022, 9998) answered HTTP 200 through `pwsh -NoProfile` | `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`), `docs(e2e): correct the weather and memory specs, and align their versions` (`5e5bd3e898e7c262bd691a89b45b49ecfb75f8c2`), `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`) and `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`) |
| F48 | ascend-agent `metadata.toolsUsed` was always empty for MCP tools the agent executes internally, and the per-tool metrics never incremented (formerly A53) | [apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/chat/ChatExecutor.java](../apps/ascend-agent/src/main/java/com/lukk/ascend/ai/agent/service/chat/ChatExecutor.java) `extractToolsUsed`. The method read `getToolCalls()` off the final assistant message, which Spring AI has already resolved by executing the tools internally, so the list was empty on every internally executed tool call. `recordToolMetrics` iterated that same empty list, so the per-tool counters stayed at zero too. A decorator on the tool-calling manager (`TrackingToolCallingManager`, `ToolCallTracker`) records each tool the loop executes, and the names are drained into the response and the metrics | `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F49 | Ten Bruno request scripts asserted less than their specs, and one spec told the runner to edit a request between runs (formerly A54) | [docs/api/request/AscendAI](api/request/AscendAI): `memory/testing/mcp-list-tools.yml`, `web-hunter/testing/mcp-list-tools.yml`, `web-hunter/testing/mcp-search.yml`, `web-hunter/testing/extract-example-com.yml`, `web-hunter/testing/session-clear-existing.yml`, `weather-mcp/forecast-warsaw-3d.yml`, `ascend-agent/testing/rag-ingestion-run.yml`, `ascend-agent/testing/prompt-cache-anthropic.yml`, `ocr/testing/ocr-unsupported-mime.yml`, `ascend-agent/testing/rag-prompt.yml`. The memory and web-hunter `tools/list` scripts checked names or any envelope where specs 4 list the input schemas, the web-hunter search script accepted any envelope where spec 5 lists 1 to 3 entries with `title`, http(s) `url` and `content`, the read script accepted any object with a `status` key where spec 3 lists `url`, `status`, a content length of at least 50 and the canary phrase, the existing-session clear never checked `cleared_cache_entries`, the forecast script never checked that the first date is today or tomorrow in UTC, the ingestion-run script asserted `indexed >= 1` for specs 5 and 7 which require 3 and 2, the Anthropic prompt-cache script accepted a write or a read on the step 2 call that spec 9 says must be a read, the unsupported-MIME request sent the text fixture with its real content type so the header lie spec 12 describes was never exercised, and spec 5 step 3 had the runner edit `rag-prompt.yml` to switch prompts, which a runner may not do. Each passed on output weaker than its spec. Every script asserts its spec's Expected list, `rag-ingestion-run.yml` and `prompt-cache-anthropic.yml` take `ragMinIndexed` and `promptCacheStep` from `--env-var` with a request-variable default, the unsupported-MIME file part declares `image/png` (confirmed on the wire), and spec 5's three prompts are three requests, `rag-prompt-markdown-canary.yml`, `rag-prompt-banana-price.yml` and `rag-prompt-pierogi-recipe.yml`. The ten free-to-run requests passed against the live stack on 2026-09-10 and the rest parse. `weather-mcp/current-warsaw.yml`, `current-warsaw-us.yml` and `session-clear-idempotent.yml` were checked in the same pass and already matched their specs | `test(bruno): assert the real contracts across the collection` (`3819db68181386831ea7959b801944637b32631a`) and `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) |
| F50 | Four spec text errors and two missing scheduling rules (formerly A55) | [apps/ascend-web-hunter/e2e/testing/1-invalid-input-test.md](../apps/ascend-web-hunter/e2e/testing/1-invalid-input-test.md), [apps/ascend-audio-scribe/e2e/testing/2-transcribe-openai-test.md](../apps/ascend-audio-scribe/e2e/testing/2-transcribe-openai-test.md), [apps/ascend-ocr/e2e/testing/2-ocr-english-test.md](../apps/ascend-ocr/e2e/testing/2-ocr-english-test.md), [apps/ascend-agent/e2e/testing/5-rag-test.md](../apps/ascend-agent/e2e/testing/5-rag-test.md), [apps/ascend-ocr/e2e/README.md](../apps/ascend-ocr/e2e/README.md), [apps/ascend-agent/e2e/README.md](../apps/ascend-agent/e2e/README.md). web-hunter 1 said each rejection returns in under 200 ms where Bruno measured 325 to 362 ms on 2026-09-10, audio 2's egress probe kept `curl -f` so it exited 22 on the 401 the prose expects, ocr 2 described its fixture as a single 120 pt line while it is a page-1 screenshot whose response carries about 30 wrapped lines, agent 5 step 3 told the runner to edit a request between runs, and the ocr and agent READMEs let the engine-bound specs (2, 3, 4, 6) and the docling-bound specs (3, 5, 6, 7) interleave with other runners, while the engine is single-threaded (A47) and a loaded host took the same OCR fixture from 59.2 s to 160.9 s on 2026-09-10, and docling peaks close to its memory limit (A3, F35). The four specs and the spec 5 template are corrected, and both READMEs carry a row in their concurrency tables plus layout prose saying each engine-bound and each docling-bound spec runs alone, with no runner of any suite active, and the Concurrency sections of ocr specs 2, 3, 4 and 6 and agent specs 3, 5, 6 and 7 state the same rule. `rag-prompt.yml` is removed, nothing referenced it after the split | `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`), `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`), `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`), `fix(agent): retry a page conversion docling dropped, report the tools a call executed, and align the version` (`df9f0a7eabe3ac56917649c54334b0ad7e52e524`) and `chore(audio-scribe): stop tracking four compiled Python files` (`27d15667649d8ae361a471d628a1f14205ba0463`) |

### ascend-web-hunter

| # | Defect | Evidence | Commit |
| :- | :--- | :--- | :--- |
| F15 | Two block pages were scored as successful scrapes, on Amazon and on Allegro | Scraper now classifies real block pages rather than returning HTTP 200 as success | `fix(web-search): make the scraper detect real block pages and stop hanging` (`0947f99ebf6cd9662460ad45069c2b243de26235`) |
| F16 | A crawler ran past its own deadline because it swallowed cancellation | Same fix, same commit ("stop hanging" half) | `0947f99ebf6cd9662460ad45069c2b243de26235` |
| F17 | A blocklist was committed into the package instead of cached | [apps/ascend-web-hunter/AGENTS.md](../apps/ascend-web-hunter/AGENTS.md), `BLOCKLIST_PATH` is now a vendored, refreshable file (default `src/assets/fanboy-annoyance.txt`) overwritten in place by `POST /api/v1/blocklist/refresh`, rather than a fetch baked into the package with no update path | `fix(web-search): stop a third-party website being able to stop the service` (`2333ba1acf9a5f059a3a4263d0002c003100162e`) |
| F18 | A blocklist was downloaded on every startup with no cache, so a third-party outage stopped the service starting | Same AGENTS.md line: `BLOCKLIST_URL` is "only reached by `POST /api/v1/blocklist/refresh`; never fetched at startup" | `2333ba1acf9a5f059a3a4263d0002c003100162e` |
| F19 | A dependency pin crashed the crawler when bumped | Not independently confirmed. No pin comment naming a crash was found in `apps/ascend-web-hunter/pyproject.toml`; recorded as reported-fixed without direct evidence in this session |  |
| F20 | A session profile argument was accepted and silently discarded | `profile` is threaded through [apps/ascend-web-hunter/src/session/session_manager.py](../apps/ascend-web-hunter/src/session/session_manager.py) and every reader strategy that consults it; current code does not discard it | Not identifiable in the available window |
| F21 | A session was reported active for days after its cookie expired | Session liveness now checks the actual cookie state rather than trusting a cached flag | `fix(web-search): stop reporting dead sessions as live, and lock the browser` (`02bacdbd3743cf1f1d36d12ad7ff02848de2edf3`) |
| F22 | Two browser intervention flows collided and silently lost the captured session | Same commit, the "lock the browser" half | `02bacdbd3743cf1f1d36d12ad7ff02848de2edf3` |
| F36 | web-hunter spec 10 reset and cleaned a session key the request no longer writes (formerly A39) | [apps/ascend-web-hunter/e2e/testing/10-session-establish-test.md](../apps/ascend-web-hunter/e2e/testing/10-session-establish-test.md), its template, [docs/api/request/AscendAI/web-hunter/testing/session-establish.yml](api/request/AscendAI/web-hunter/testing/session-establish.yml). Named `session:example.net:default` while the request sends `profile=e2e-establish` and the service forwards it since 2026-09-08, so the capture lands under `session:example.net:e2e-establish`. Measured on 2026-09-09: that key existed after the run while the spec's cleanup deleted the `default` key and reported nothing. Every key in the spec, the template and the request comment is renamed, and the stranded key was removed (`DEL` returned `1`) | `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`) and `test(bruno): assert the real contracts across the collection` (`3819db68181386831ea7959b801944637b32631a`) |
| F37 | web-hunter spec 8 reset Redis only, so the in-process read cache spec 3 fills survived into the call under test (formerly A40) | [apps/ascend-web-hunter/e2e/testing/8-session-clear-test.md](../apps/ascend-web-hunter/e2e/testing/8-session-clear-test.md). Observed `cleared_cache_entries` equal to `1` instead of the asserted `0` on the idempotent call after spec 3 had read `example.com`. Redis deletes cannot reach the service's per-domain read cache. Reset state now primes the clear endpoint for `example.com` first | `fix(web-hunter): make specs 6, 8 and 10 reproducible, and align the version` (`a8b7e3bf3958dd9f97f51e8b8d6125880751152a`) |
| F47 | Observation, no product defect: web-hunter spec 6's Cloudflare row failed once on 2026-09-10 during a reported home network cut, and the labelled rerun passed (formerly A52) | [apps/ascend-web-hunter/e2e/testing/6-tiered-scraping-test.md](../apps/ascend-web-hunter/e2e/testing/6-tiered-scraping-test.md), FlareSolverr v3.5.0 in [compose.ascend-web-hunter.yaml](../compose.ascend-web-hunter.yaml). First run: FlareSolverr answered `Error solving the challenge. Timeout after 60.0 seconds` for the exact URL, both through the service and from a direct call, the service escalated to 428, and its background monitor captured a session two minutes later. The owner reports a short home network cut at that time. Labelled rerun at `2026-09-10T11-00-00`: row 2 was served through the service's Cloudflare tier in 35.5 s, while a bare `requests.get` against FlareSolverr's `/v1` for the same URL still answered a 60 s solve timeout immediately before it. The bare FlareSolverr call is not the same path as the service's Cloudflare tier: the tier injects the cookies its cookie manager stored for the domain into the FlareSolverr payload (`apps/ascend-web-hunter/src/reader/strategies/flaresolverr_strategy.py`), and a bare call sends none, so it is not a valid probe for that row. The spec's own prerequisite health check is the probe | Closed without a code change. The labelled rerun on 2026-09-10 at `2026-09-10T11-00-00` passed, and the observation is recorded in `docs: register A36 to A55, and record two full end-to-end sweeps` (`4f46d9095b9d1367385f13dbd6eb953155035add`) |

### ascend-ocr

All seven of the rows below (F23-F29) share one deployment caveat, stated once here rather than seven times: the
fixes are complete in source and covered by tests (`openspec/changes/stop-ocr-getting-stuck-on-large-jobs/tasks.md`,
every section but 1.2, 1.3, 1.4, 1.6, and 8.7 checked off), but as of task 8.8's own live-verification note the
running `ascend-ocr` container was still built from pre-fix code, and the change has not been archived into
`openspec/specs/`. Read Fixed here as fixed in source, not yet confirmed rebuilt and deployed.

| # | Defect | Evidence | Commit |
| :- | :--- | :--- | :--- |
| F23 | An OCR request timeout abandoned work while the worker ran for thirty more minutes | [apps/ascend-ocr/src/service/ocr_service.py](../apps/ascend-ocr/src/service/ocr_service.py), the worker now consumes `predict_iter()` page by page and checks its own cooperative deadline between pages instead of being cancelled out from under a still-running `ProcessPoolExecutor` job. Deployment caveat above applies | `fix(paddle-ocr): stop abandoning work and let one page cost four gigabytes less` (`5bb86191855cb1173ff8c3b4031958acc7c0af0b`) |
| F24 | An OCR call cost roughly ten gigabytes regardless of input | `OCR_DETECTOR_MAX_SIDE` now defaults to `1536` in [apps/ascend-ocr/src/config/config.py](../apps/ascend-ocr/src/config/config.py), measured at 8.80 GiB peak for one A4 page (task 8.9), down from the unbounded 11.0 GiB baseline. Deployment caveat above applies | `5bb86191855cb1173ff8c3b4031958acc7c0af0b` |
| F25 | An OCR image path had no pixel limit at all | [apps/ascend-ocr/src/api/limits.py](../apps/ascend-ocr/src/api/limits.py), header-only pixel inspection enforced at both REST and MCP boundaries before any decode, `OCR_MAX_INFERENCE_PIXELS` defaulting to 2,500,000. Deployment caveat above applies | `5bb86191855cb1173ff8c3b4031958acc7c0af0b` |
| F26 | OCR readiness reported healthy for five hours with no worker process | [apps/ascend-ocr/src/observability/metrics.py:81-103](../apps/ascend-ocr/src/observability/metrics.py), `is_engine_warm()` reads the same cross-process metric the worker itself writes, and `/ready` composes `is_pool_usable()`, `is_rebuild_in_progress()`, and `is_job_overrunning()`. Deployment caveat above applies | `5bb86191855cb1173ff8c3b4031958acc7c0af0b` |
| F27 | Two OCR language codes were advertised but always failed | [apps/ascend-ocr/src/config/config.py:64-66](../apps/ascend-ocr/src/config/config.py), `SUPPORTED_LANGUAGES` now uses PaddleOCR's own internal codes (`ch`, `japan`, `korean`) rather than ISO codes the underlying library does not recognise | `fix(paddle-ocr): serve the two languages it advertised but could not read` (`d92c2b6e59b19e5a7c379a985c2a9ee0adcd2600`) |
| F28 | An OCR cache eviction log and metric were invisible because they lived in the wrong process | [apps/ascend-ocr/src/observability/metrics.py:1-2,86-90](../apps/ascend-ocr/src/observability/metrics.py), `ENGINE_CACHE_EVICTIONS_TOTAL` is now read back through `prometheus_client.multiprocess.MultiProcessCollector` against `PROMETHEUS_MULTIPROC_DIR`, the same cross-process mechanism `is_engine_warm()` uses, so a counter incremented inside the `ProcessPoolExecutor` worker (`ocr_service.py:183-187`) reaches `/metrics` in the parent | Not identifiable to a single commit; consistent with `5bb86191855cb1173ff8c3b4031958acc7c0af0b` |
| F29 | An OCR warm-up built an engine nothing used, while readiness reported on it | Same `is_engine_warm()` mechanism as F28: readiness now reads the real worker's own warm-up metric rather than a separate engine instantiated only to answer the readiness check | Consistent with `5bb86191855cb1173ff8c3b4031958acc7c0af0b` |
| F30 | The OCR service starved its own event loop through the interpreter lock | [apps/ascend-ocr/src/service/ocr_service.py](../apps/ascend-ocr/src/service/ocr_service.py), inference already runs inside a `ProcessPoolExecutor`, isolated from the API process's event loop. This is confirmed as currently true, but the architecture appears to predate this session's work (the large OCR change treats the process pool as pre-existing infrastructure it builds on, not something it introduces), so this may not be a "past several days" fix at all | Not identifiable; likely predates the available reflog window entirely |
| F33 | OCR spec 2 timed out on a one-page document because compose raised the request ceiling but never the per-page allowance (formerly A36) | [compose.yaml](../compose.yaml) `ascend-ocr` environment, [apps/ascend-ocr/e2e/testing/2-ocr-english-test.md](../apps/ascend-ocr/e2e/testing/2-ocr-english-test.md). `OCR_REQUEST_TIMEOUT` was raised to 300 but `OCR_PAGE_TIMEOUT_SECONDS` was never set, so a one-page document kept the 120 s default budget, `min(1 * 120, 300)`. Measured on 2026-09-09: two runs timed out at 138.850 s and 134.004 s elapsed against a 119.997 s budget, and measured runtimes for the same fixture span 81 to 139 s. compose now sets `OCR_PAGE_TIMEOUT_SECONDS=150` beside `OCR_REQUEST_TIMEOUT=300`. The 150 s budget was not the whole fix: on 2026-09-10 the labelled rerun on a quiet host took 59.2 s while the loaded run of the same fixture took 160.9 s, past even the raised budget, so the closing fix is the scheduling rule recorded under F50, an engine-bound spec runs alone with no runner of any suite active | `fix(ocr): give one page 150 seconds, read the version from the package, and run engine specs alone` (`4ac41bb6d03db4c4e814f72d5bddbbc1b08a9e95`) |

### ascend-audio-scribe

| # | Defect | Evidence | Commit |
| :- | :--- | :--- | :--- |
| F34 | ascend-audio-scribe answered 400 to an upstream provider failure (formerly A37) | [apps/ascend-audio-scribe/src/transcription/huggingface_api_speach_to_text.py](../apps/ascend-audio-scribe/src/transcription/huggingface_api_speach_to_text.py), [apps/ascend-audio-scribe/src/api/exception_handlers.py](../apps/ascend-audio-scribe/src/api/exception_handlers.py). Measured from inside the container: `https://router.huggingface.co/hf-inference/models/openai/whisper-large-v3` answered 504 after 124 s. The provider module re-raised that as `ValueError`, and the exception handler mapped `ValueError` to 400, so a provider outage was reported as the caller's fault. Both hosted providers now raise one dedicated upstream exception mapped to 502 on the REST and streaming paths | `fix(audio-scribe): answer 502 when a provider fails, and read the version from the package` (`a552c9de9ebcb1085015b5a9eba257b2728b1dde`) |
| F43 | Four compiled Python files were tracked in git under `apps/ascend-audio-scribe/src` (formerly A48) | `src/__pycache__/__init__.cpython-311.pyc`, `src/__pycache__/scribe.cpython-311.pyc`, `src/transcription/__pycache__/__init__.cpython-311.pyc`, `src/transcription/__pycache__/openai_api_speach_to_text.cpython-311.pyc`. Tracked since commit 129c8ce despite the `__pycache__/` ignore rule (`git ls-files` listed all four, `git log --diff-filter=A` dated each to 129c8ce), so every local run that recompiled them showed as a modification and the repository carried one interpreter's bytecode. Untracked with `git rm --cached`, the files stay on disk and git no longer sees them | `chore(audio-scribe): stop tracking four compiled Python files` (`27d15667649d8ae361a471d628a1f14205ba0463`) |
| F46 | Three transcribe Bruno requests asserted JSON keys on a markdown `FileResponse` (formerly A51) | [docs/api/request/AscendAI/transcribe/huggingface.yml](api/request/AscendAI/transcribe/huggingface.yml), `transcribe/local.yml`, `transcribe/openai.yml`. The endpoints answer a `text/markdown` attachment (`_file_response` in `apps/ascend-audio-scribe/src/api/rest/rest_endpoints.py`, and the sibling `transcribe/testing/*-canary.yml` requests assert exactly that), so the scripts' `JSON.parse` of the body threw and every successful transcription failed the test. The scripts assert HTTP 200, a `text/markdown` Content-Type, the `attachment; filename="transcript_<provider>.md"` Content-Disposition and a non-empty body. Not run, since each call bills a hosted provider | `test(bruno): assert the real contracts across the collection` (`3819db68181386831ea7959b801944637b32631a`) |

### Not independently verifiable

| # | Defect | Status |
| :- | :--- | :--- |
| F31 | The audio service double-encoded its tool responses | Not re-verified against current source. `AudioScribe`'s rename to `ascend-audio-scribe` completed live during this verification session, and that module is explicitly out of scope to touch per this task's boundaries. Carried forward as fixed from the prior report; nobody has re-confirmed it against the module's current code in this pass |

---

## What changed underneath this register while it was being written

`AudioScribe` became `ascend-audio-scribe` mid-session, the same rename `AscendWebSearch` went through earlier.
`.gitignore` was already updated to match by the time this register was finished (see the verification note above).
`.github/workflows/ci.yaml` and `.github/workflows/release.yaml` were read before that rename completed and, at
read time, still referenced the `AudioScribe` path (`"path":"AudioScribe"` in the CI matrix, `AudioScribe/pyproject.toml`
in the release manifest reader). Whether that has since been corrected by the same concurrent session was not
re-checked, since re-reading those files was outside this task's scope once the rename was observed in progress.
Whoever owns that rename should confirm `ci.yaml` and `release.yaml` both resolve against `apps/ascend-audio-scribe/`
before the next push or release dispatch, since a stale path there means the audio service silently stops being
built and tested, not merely mislinted.

Follow-up, same day: both files were reread while closing F32 below. Neither carries a stale `AudioScribe` or
`AscendWebSearch` path any longer: [.github/workflows/ci.yaml](../.github/workflows/ci.yaml) matches on
`apps/ascend-audio-scribe/**` and `apps/ascend-web-hunter/**` throughout its filter, matrix, and toolchain steps, and
[.github/workflows/release.yaml](../.github/workflows/release.yaml) reads
`apps/ascend-audio-scribe/CHANGELOG.md` and `apps/ascend-web-hunter/CHANGELOG.md` in both the `prepare` and `release`
jobs. Both files were rewritten wholesale for the change that closes F32, so this could be the same edit resolving
it incidentally rather than a targeted fix. Either way, the concern this note flagged does not hold against either
file as they stand right now, uncommitted.
