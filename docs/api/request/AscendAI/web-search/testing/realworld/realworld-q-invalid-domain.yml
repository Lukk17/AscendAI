info:
  name: realworld-q-invalid-domain
  type: http
  seq: 17

http:
  method: POST
  url: "{{protocol}}://{{baseUrl}}:7021/api/v2/web/read"
  body:
    type: json
    data: |-
      {
        "url": "https://this-domain-does-not-exist-xyzzy.invalid/",
        "include_links": false,
        "heavy_mode": false
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("hard-fail: SSRF guard fail-closes with HTTP 400", function () {
            expect(res.getStatus()).to.equal(400);
        });
        test("hard-fail: response is not a success", function () {
            expect(res.getBody()?.status).to.not.equal("success");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
