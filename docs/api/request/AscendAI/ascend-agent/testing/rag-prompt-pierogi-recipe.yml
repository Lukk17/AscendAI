info:
  name: rag-prompt-pierogi-recipe
  type: http
  seq: 19

http:
  method: POST
  url: "{{protocol}}://{{baseUrl}}:9917/api/v1/ai/prompt"
  headers:
    - name: X-User-Id
      value: frostyRagTest
  body:
    type: multipart-form
    data:
      - name: prompt
        type: text
        value: How long should I rest the pierogi dough according to Babcia Helena's recipe?
      - name: embeddingProvider
        type: text
        value: openai
      - name: provider
        type: text
        value: minimax
      - name: model
        type: text
        value: MiniMax-M2.7

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function () {
            expect(res.getStatus()).to.equal(200);
        });
        test("Answer carries the DOCX fixture's rest time and is not a refusal", function () {
            const content = res.getBody().content;
            expect(content).to.be.a("string").and.not.empty;
            expect(content).to.include("30 minutes");
            expect(content.toLowerCase()).to.not.include("i don't have that document");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
