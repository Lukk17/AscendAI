info:
  name: transcribe-openai-canary
  type: http
  seq: 2

http:
  method: POST
  url: "{{protocol}}://{{baseUrl}}:7017/api/v1/transcribe/openai"
  headers:
    - name: accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - "{{audioScribeFixtures}}/meeting-clip.wav"
      - name: model
        type: text
        value: whisper-1
      - name: language
        type: text
        value: en
      - name: stream
        type: text
        value: "false"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function () {
            expect(res.getStatus()).to.equal(200);
        });
        test("Content-Type starts with text/markdown", function () {
            expect(res.getHeader("content-type")).to.match(/^text\/markdown/);
        });
        test("Content-Disposition names transcript_openai.md", function () {
            expect(res.getHeader("content-disposition")).to.equal("attachment; filename=\"transcript_openai.md\"");
        });
        test("Transcript body carries the canary phrase", function () {
            const body = String(res.getBody());
            expect(body.length).to.be.at.least(5);
            const lower = body.toLowerCase();
            const canaries = ["q3", "acme", "adam", "friday", "migration"];
            expect(canaries.some(function (c) { return lower.includes(c); })).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
