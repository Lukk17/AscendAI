# AscendWebSearch e2e login credentials. Copy to `.env.local` (gitignored) and fill in.
# One USER/PASS pair per login-walled service the seed harness logs into.
# URLs and DOM selectors are hardcoded in the e2e tests, not here.

SAUCEDEMO_USER=standard_user
SAUCEDEMO_PASS=secret_sauce
