Group 1 is a hard gate. It proves Floci satisfies the S3 contract the code already assumes, and if it does not, the approach in design.md changes and nothing after it is worth doing. Run it first, alone.

Once group 1 passes, groups 2 through 9 touch disjoint file sets and can be handed to separate implementers working in parallel: group 2 and group 3 together are AscendAgent Java and configuration (3 depends on 2's banner rename, so one implementer takes both), group 4 is the Python services, group 5 is `observability/`, group 6 is `docker-compose.yaml`, group 7 is the e2e runbooks, group 8 is documentation, group 9 is `openspec/`. Group 10 runs last, after everything else has landed. Nothing is gated on an unanswered question any more: the four judgement calls are settled in the proposal's Settled Decisions section and written into design.md decisions 5, 6 and 7.

## 1. Verify the Floci contract before changing anything

- [x] 1.1 Probe the unauthenticated S3 verbs the runbook rewrite depends on, against the live Floci on port 9070 and a scratch bucket named `floci-contract-probe`, never `knowledge-base` and never `local-dev-bucket`. Verify that `PUT` of the bucket returns 200, `PUT` of an object into it returns 200, `GET` of that object returns the exact bytes written, `?list-type=2` returns `ListBucketResult` and honours `&prefix=`, `DELETE` of the object returns 204, `DELETE` of the emptied bucket returns 204, `GET http://localhost:9070/_floci/health` returns 200 with `"s3":"running"` in its JSON body, and a request for a missing key returns HTTP 404 carrying S3 error XML with `<Code>NoSuchKey</Code>` so a runbook can tell "absent" from "broken". Record every command and its exact status in `implementation-notes.md`. If any verb rejects the unauthenticated request, stop and apply the fallback in design.md decision 2 before continuing.
- [x] 1.2 Verify Floci implements `CreateBucket` the way `BucketInitConfig` calls it, against a Floci that has no `knowledge-base` bucket. Start the agent with `./gradlew bootRun`, then verify `curl -fsS "http://localhost:9070/knowledge-base?list-type=2"` returns a `ListBucketResult` rather than `NoSuchBucket`, and that the startup banner reports the S3 check as reachable.
- [x] 1.3 Verify a presigned URL produced by the running agent resolves against Floci. Upload one document via `POST /api/v1/ingestion/upload`, run `POST /api/v1/ingestion/run`, send a prompt with `attachSources=true`, and verify a plain `curl` of the returned `downloadUrl` returns HTTP 200 with the file bytes and that the URL carries an `X-Amz-Signature` query parameter.
- [x] 1.4 Verify the object-listing shape the runbooks will assert on. Verify `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=documents/"` returns XML whose `<Key>` elements name the objects uploaded in 1.3, and record the exact element path so runbook assertions in group 7 cite it rather than guessing.
- [x] 1.5 Delete the scratch object and the scratch bucket created in 1.1, and verify `curl -fsS http://localhost:9070/` no longer lists `floci-contract-probe` while `local-dev-bucket` is still present with an unchanged creation date and its objects intact. `knowledge-base` joins that listing only once 1.2 has run and the agent has created it.

## 2. AscendAgent Java and runtime configuration

- [x] 2.1 Rename the MinIO references in `AscendAgent/src/main/resources/application.yaml`: the `public-endpoint` comment in the `app.s3` block at `:91-100` and the presigned-URL comment at `:283-285`. Leave every value untouched. Verify `git diff` on the file shows comment lines only and `./gradlew bootRun` still starts with the S3 check reachable.
- [x] 2.2 Rename the MinIO reference in the comment at `AscendAgent/src/main/resources/application-docker.yaml:8-13` to name Floci and the external port-9070 prerequisite. Verify `git diff` shows a comment-only change.
- [x] 2.3 Rename the comment at `AscendAgent/src/main/java/com/lukk/ascend/ai/agent/config/AppConfig.java:104` so it explains that the S3 client requires a region and the local emulator reports `us-east-1`. Verify `./gradlew compileJava` succeeds and `git diff` shows one comment line.
- [x] 2.4 Change the startup banner label at `AscendAgent/src/main/java/com/lukk/ascend/ai/agent/config/StartupLogConfig.java:126` from `"      S3 (MinIO):   "` to `"      S3 (Floci):   "`, preserving the exact column padding so the banner stays aligned. Verify `./gradlew bootRun` prints a banner whose S3 line aligns with the neighbouring lines.
- [x] 2.5 Verify no MinIO reference remains under `AscendAgent/src/main/`: `rg -i minio AscendAgent/src/main/` returns nothing.

## 3. AscendAgent tests

- [x] 3.1 Replace the `MinIOContainer` at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/integration/TestcontainersBase.java:57-59` with a `GenericContainer` on `DockerImageName.parse("floci/floci:2.0.1")` exposing `4566`, with a readiness wait on `GET /_floci/health` returning 200 on the mapped port, and remove the `org.testcontainers.containers.MinIOContainer` import at `:8`. Verify `./gradlew compileTestJava` succeeds.
- [x] 3.2 Rewrite the three `registry.add` calls at `TestcontainersBase.java:80-82` to build the endpoint from `getHost()` and `getMappedPort(4566)` and to supply the literal credentials `admin` / `password` so test wiring matches `application.yaml`. Rename the `// MinIO` section comment at `:79` and the class Javadoc at `:18`. Verify `./gradlew integrationTest --tests "*BackingServicesIT"` passes.
- [x] 3.3 Remove `testcontainers-minio` from `AscendAgent/gradle/libs.versions.toml:91` and `AscendAgent/build.gradle.kts:105`. Verify `./gradlew dependencies --configuration testRuntimeClasspath | rg -i minio` returns nothing and `./gradlew compileTestJava` still succeeds.
- [x] 3.4 Update the banner assertion at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/integration/StartupBannerIT.java:80` to `assertThat(banner).contains("S3 (Floci):")` and the failure-simulation string at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/config/StartupLogConfigTest.java:208` to a product-neutral message. Verify `./gradlew test --tests "*StartupLogConfigTest"` and `./gradlew integrationTest --tests "*StartupBannerIT"` both pass.
- [x] 3.5 Rename `minio_isReachableAndBucketCreated` at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/integration/BackingServicesIT.java:73` to `objectStore_isReachableAndBucketCreated` and rename the class Javadoc at `:19`. Verify `./gradlew integrationTest --tests "*BackingServicesIT"` passes and the report lists the new method name.
- [x] 3.6 Rename the MinIO references in the comments, Javadoc, and test-method name at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/integration/IngestionEndToEndIT.java:44,49,93,99,131,140,156,188`, including `upload_acceptsMarkdownPdfAndDocxInOneRequest_andStoresInMinio`. Verify `./gradlew integrationTest --tests "*IngestionEndToEndIT"` passes against the Floci container.
- [x] 3.7 Add an integration assertion that a presigned URL resolves against Floci, covering the `rag-source-attachments` scenario "URL resolves against an endpoint that does not validate credentials". Verify the new test fails when pointed at an unreachable endpoint and passes against the Floci container.
- [x] 3.8 Replace the `minioadmin` / `minioadmin` fixtures at `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/config/AppConfigVectorStoreInitTest.java:132-135` with `admin` / `password`, and the `https://minio.example/...` placeholder hosts in `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/service/rag/S3PresignedUrlServiceTest.java:67,114,152,199,228` with `https://s3.example/...`. Verify `./gradlew test --tests "*AppConfigVectorStoreInitTest" --tests "*S3PresignedUrlServiceTest"` passes.
- [x] 3.9 Rename the three MinIO references in `AscendAgent/src/test/java/com/lukk/ascend/ai/agent/integration/README.md:3,99,102` to name Floci and the `knowledge-base` bucket. Verify the table rows still describe what each integration test asserts.
- [ ] 3.10 Record the wall-clock duration of the full suite before and after the container swap so a Floci startup regression is visible. Verify `./gradlew integrationTest` completes and the two durations are written into the change's implementation notes.
  - Closed as not measurable on 2026-09-04, and archived unticked rather than ticked, because it was never satisfied. No baseline was captured before the swap, and reconstructing one at the pre-migration commit would measure the wrong thing: that code carried the test-isolation defect fixed under task 10.2, which restarted every backing container between test classes while cached Spring contexts stayed wired to the dead ports, so its runs are dominated by connection timeouts and by six-to-seven container startup sets per run rather than by object-store cost. The post-swap durations in `implementation-notes.md` stand as the forward baseline for detecting a future Floci regression. Full reasoning is in the "Task 3.10" section of that file.
- [x] 3.11 Verify no MinIO reference remains under `AscendAgent/src/test/`, `AscendAgent/build.gradle.kts`, or `AscendAgent/gradle/`: `rg -i minio AscendAgent/src/test/ AscendAgent/build.gradle.kts AscendAgent/gradle/` returns nothing.

## 4. Python services

- [x] 4.1 Rename the MinIO references in the comment at `AudioScribe/src/config/config.py:79-82` and the docstrings at `AudioScribe/src/adapters/download_service.py:19-22,61-63`. Verify `pytest` passes in `AudioScribe/` and `ruff check .` is clean.
- [x] 4.2 Replace the literal `"minio"` test hostname in `AudioScribe/tests/config/test_config.py` and `AudioScribe/tests/adapters/test_download_service.py` with a product-neutral hostname. Verify `pytest AudioScribe/tests/config/test_config.py AudioScribe/tests/adapters/test_download_service.py` passes.
- [x] 4.3 Update the object-store URL at `AudioScribe/mcp_requests.http:1,4` to a Floci-served URL on port 9070. Verify the request returns HTTP 200 when executed against the running stack with the fixture present.
- [x] 4.4 Rename the MinIO reference in the comment at `PaddleOCR/AGENTS.md` describing `MCP_ALLOWED_HOSTS`, and in `PaddleOCR/docs/architecture/decisions/ADR-001-mcp-file-transport-uri-only.md` (7 occurrences). The two files sit on opposite sides of design.md decision 6: `PaddleOCR/AGENTS.md` states an operator prerequisite and names Floci with its port, while the ADR is architecture-level and says S3-compatible object storage, naming Floci only as an aside. Verify the ADR still reads coherently about the docker-internal fetch pattern that motivated the allowlist.
- [x] 4.5 Verify no MinIO reference remains in the Python source or test trees: `rg -i minio AudioScribe/src/ AudioScribe/tests/ PaddleOCR/src/ PaddleOCR/tests/` returns nothing.

## 5. Observability

- [x] 5.1 Delete the `minio` and `minio-bucket` scrape jobs at `observability/prometheus/prometheus.yaml:76-92`. Verify the file still parses (`docker compose config` reports no error for the prometheus service) and that the remaining job list contains no `minio` entry.
- [x] 5.2 Delete the MinIO row panel and its two timeseries panels at `observability/grafana/dashboards/infrastructure.json:70-124`, and remove the MinIO clause from the dashboard description at `:8`. Verify the file is valid JSON (`python -m json.tool observability/grafana/dashboards/infrastructure.json > /dev/null` exits 0) and that the dashboard renders in Grafana with no empty row.
- [x] 5.3 Remove the MinIO references from `observability/README.md` (5 occurrences), stating plainly that the object store publishes no metrics and that its liveness signal is `GET http://localhost:9070/_floci/health`. Verify the scrape-target list in the README matches the job list in `prometheus.yaml` exactly.
- [x] 5.4 Remove the MinIO reference from `docs/OBSERVABILITY.md`. Verify the document's target inventory matches `prometheus.yaml`.
- [x] 5.5 Verify no target with `job="minio"` is reported by Prometheus after a reload, and that the infrastructure dashboard shows no panel querying a `minio_*` metric.

## 6. Docker Compose

- [x] 6.1 Remove the literal `minio` entry from `MCP_ALLOWED_HOSTS` at `docker-compose.yaml:236`, leaving `host.docker.internal,localhost,127.0.0.1`. Verify `docker compose config` renders the service without error and the rendered value contains no `minio`.
- [x] 6.2 Rewrite the SSRF-allowlist comments at `docker-compose.yaml:65-67` and `:233-236` to explain the allowlist in terms of the host-published Floci endpoint on port 9070 rather than a docker-internal MinIO hostname. Verify `git diff` shows comment lines plus the single value change from 6.1.
- [x] 6.3 Verify no compose file defines an object-store service, satisfying the `ascend-agent-containerization` scenario "No compose file defines the object store": `rg -i "minio|floci" docker-compose.yaml ascend-scrapper.docker-compose.yaml` returns nothing.
- [x] 6.4 Verify PaddleOCR still fetches a Floci-hosted object through its MCP tool after the allowlist change, by calling `ocr_process` with a `http://host.docker.internal:9070/e2e-fixtures/<key>` URI and confirming it returns extracted text rather than `UNSAFE_URI`.

## 7. Operator runbooks

Gated on group 1, which has passed. Runbooks are operator-facing, so they name Floci concretely. Destructive steps follow design.md decision 5: deleting an object, emptying a prefix, and deleting a whole bucket are all allowed as long as the command names `knowledge-base` or `e2e-fixtures` literally, and no step may use a wildcard, delete every bucket, or enumerate the endpoint's buckets and remove what it finds, because this instance also holds `local-dev-bucket` from another repository's compose project.

- [x] 7.1 Rewrite the object-store section of `docs/TROUBLESHOOTING.md:39-55` using the command mapping in design.md decision 2. Drop `mc alias set local http://localhost:9000 admin password`, which has no equivalent. Keep the bucket reset recipe that `mc rb --force local/knowledge-base` provided, expressed as a delete of the objects under `knowledge-base` followed by `curl -sS -X DELETE http://localhost:9070/knowledge-base` with the bucket named literally, and say that the agent recreates the bucket on its next start through `BucketInitConfig`. Replace the `/minio/health/live` probe with `/_floci/health`. Verify every command in the rewritten section runs successfully against the live stack, and that no command in it is unscoped.
- [x] 7.2 Rewrite the reset, seed, verify, and cleanup steps in `AscendAgent/e2e/testing/5-rag-test.md`, `6-attach-sources-test.md`, and `7-rag-dedup-test.md`, and the MinIO mention in `8-prompt-cache-openai-test.md`. Verify each spec's full command list executes end to end against the live stack with no `mc` and no container name.
- [x] 7.3 Apply the same rewrite to the three sidecar templates at `AscendAgent/e2e/testing/templates/5-rag-tasks.template.md`, `6-attach-sources-tasks.template.md`, and `7-rag-dedup-tasks.template.md`. Verify each template's commands are byte-identical to the corresponding spec's, since the template is what a runner copies.
- [x] 7.4 Rewrite `PaddleOCR/e2e/testing/6-mcp-ocr-test.md` and `PaddleOCR/e2e/testing/templates/6-mcp-ocr-tasks.template.md`, including the `e2e-fixtures` bucket creation and the seed upload, dropping the `mc anonymous set download` step per design.md decision 2. Verify the fixture uploads, the MCP `ocr_process` call returns text, and the cleanup removes the object.
- [x] 7.5 Rewrite `AudioScribe/e2e/testing/5-mcp-transcribe-test.md` and `AudioScribe/e2e/testing/templates/5-mcp-transcribe-tasks.template.md` the same way. Verify the fixture uploads, the MCP transcription call returns a transcript, and the cleanup removes the object.
- [x] 7.6 Update the five e2e README files that describe the object store: `AscendAgent/e2e/README.md` (16 occurrences, including the mermaid node `S3[(MinIO :9070)]`, the concurrency-group rationale, the prerequisite list, the capability table, and the three `Bash(docker exec minio ...)` allowlist entries in the Claude Code permission section), `AscendAgent/e2e/testing/README.md`, `PaddleOCR/e2e/testing/README.md`, `AudioScribe/e2e/README.md`, and `WeatherMCP/e2e/README.md`. Verify the permission allowlist entries in `AscendAgent/e2e/README.md` name commands that the rewritten specs actually issue.
- [x] 7.7 Verify no runbook still references the removed tooling: `rg -i "minio|\bmc \b|minio/health" AscendAgent/e2e/ AudioScribe/e2e/ PaddleOCR/e2e/ WeatherMCP/e2e/ docs/TROUBLESHOOTING.md --glob '!**/runs/**'` returns nothing. Historical run records under `*/e2e/testing/runs/` are deliberately excluded and stay untouched.

## 8. Documentation sweep

The sweep has two halves with opposite naming rules, per design.md decision 6.

Tasks 8.1 through 8.5 are operator-facing. They name Floci concretely, with its ports, because the reader is about to open something or type something and needs to know what.

Tasks 8.6 through 8.9 are architecture-level. They do not name the vendor in the running prose. The prerequisite is described as S3-compatible object storage, and the concrete implementation appears only as an aside, for example "S3-compatible object storage, provided locally by Floci and by Amazon S3 in production". The reason is that the vendor is a local-development detail that has already changed once and will change again, and an architecture document that hardcodes it goes stale on the next swap.

Operator-facing:

- [x] 8.1 Update `README.md` (8 occurrences): the badge, the architecture diagram node, the prerequisite line at `:265`, and the port and credential table row at `:421`. Name Floci with both ports `9070` and `9071` and note that the UI on 9071 replaces the MinIO console. Verify every link in the touched sections resolves and the port table matches `AGENTS.md`.
- [x] 8.2 Update `AscendAgent/README.md` (10 occurrences), including the console instructions around `:147` and the `mc` guidance at `:168-193`, satisfying the `rag-documentation` delta. Verify a reader following the upload instructions verbatim lands a file in `knowledge-base` and a subsequent `POST /api/ingestion/run` indexes it.
- [x] 8.3 Update `AscendAgent/docs/CONFIGURATION.md:117-124`, `docs/INGESTION.md`, and `docs/DEPLOYMENT.md:158`. Verify the `app.s3` property documentation matches `application.yaml` value for value.
- [x] 8.4 Update the external-prerequisite tables in `AGENTS.md` and `AscendAgent/AGENTS.md`. Verify both tables list the same ports and the same product name as `README.md`.
- [x] 8.5 Update the module read-me files `PaddleOCR/README.md` and `AudioScribe/README.md`. Verify their port and prerequisite statements match the root `README.md`.

Architecture-level:

- [x] 8.6 Rename MinIO in place inside `docs/architecture/decisions/ADR-M003-external-infrastructure-prerequisites.md:9,16` and its index entry at `docs/architecture/decisions/README.md:11`, which currently reads "Infrastructure services (Redis, Qdrant, MinIO) as external prerequisites". Do not write an ADR-M004 and do not mark ADR-M003 superseded: the decision it records is the pattern of keeping infrastructure outside compose, and the product behind the S3 API is an implementation detail of that pattern. Both the context and the decision describe the prerequisite as S3-compatible object storage and name Floci only as an aside. Verify the ADR index entry and the ADR title agree, and that the ADR still reads as one coherent decision rather than a find-and-replace.
- [x] 8.7 Update the monorepo architecture documents: `docs/architecture/diagrams/system-overview.md`, and the arc42 chapters `02-system-context.md`, `03-building-blocks.md`, `04-deployment.md`, `05-crosscutting-concerns.md`. Verify every mermaid diagram still renders and no diagram node carries a vendor name.
- [x] 8.8 Update the AscendAgent architecture documents: `docs/architecture/diagrams/deployment-diagram.md`, `container-diagram.md`, and the arc42 chapters `02-constraints.md`, `03-context-and-scope.md`, `05-building-block-view.md`, `07-deployment-view.md`, `08-crosscutting-concepts.md`, `10-quality-requirements.md`, plus `AscendAgent/docs/architecture/decisions/ADR-007-ingestion-auto-default-off.md`. Verify every mermaid diagram still renders and no diagram node carries a vendor name.
- [x] 8.9 Update the PaddleOCR and AudioScribe architecture documents: `PaddleOCR/docs/architecture/README.md`, `diagrams/container-diagram.md`, arc42 `03-context-and-scope.md`, `06-runtime-view.md`, `07-deployment-view.md`, `11-risks-and-technical-debt.md`, `12-glossary.md`, and `AudioScribe/docs/architecture/decisions/ADR-001-uri-only-mcp-transport.md`. Verify every mermaid diagram still renders and that the PaddleOCR glossary defines the object store by its contract rather than by a product name.

## 9. OpenSpec artifacts

- [x] 9.1 Update the `## Purpose` line at `openspec/specs/ai-driven-e2e-runner/spec.md:5`, which names MinIO and cannot be changed through a delta. Verify `openspec validate ai-driven-e2e-runner --strict` still passes.
- [x] 9.2 Update the MinIO reference in `openspec/changes/update-docs-and-architecture/specs/platform-documentation/spec.md`, since that change owns the documentation contract this change is executing against. Verify `openspec validate update-docs-and-architecture --strict` passes.
- [x] 9.3 Correct only the two pending changes that directly contradict what this change ships, and leave the other nine that mention MinIO untouched. Rename `MINIO_SECRET_KEY` and the `MCP_ALLOWED_HOSTS` default of `minio` in `openspec/changes/harden-cloud-deployment/` (`design.md:95`, `specs/production-deployment/spec.md:54,98,102`, `tasks.md:25,45,47`), and remove the `minio` scrape target from `openspec/changes/add-observability/specs/observability-stack/spec.md:5,11,99` plus its companions in that change's `proposal.md`, `design.md`, and `tasks.md`. Do not touch `add-document-management-api`, `add-document-connectors`, `add-tenant-isolation`, `add-audit-and-gdpr-compliance`, `add-chat-streaming-and-conversations`, `enhance-web-search-crawl-at-scale`, `add-github-actions-pipeline`, `add-web-search-scraping-e2e`, or `update-docs-and-architecture` beyond what task 9.2 already covers. Verify `openspec validate harden-cloud-deployment --strict` and `openspec validate add-observability --strict` both pass.

## 10. Final verification

- [x] 10.1 Run a repository-wide sweep and verify the only remaining MinIO references are the deliberately excluded ones. Verify `rg -il minio . --glob '!**/runs/**'` returns exactly: the 12 files under `openspec/changes/archive/`, `.agents/skills/e2e-runbooks/SKILL.md`, `openspec/schemas/e2e-runbooks/README.md`, `openspec/schemas/e2e-runbooks/templates/proposal.md`, `AscendWebSearch/src/assets/fanboy-annoyance.txt`, and the files belonging to the nine pending changes that settled decision 2 leaves alone. Any other file is an unfinished task. The three generated agent-standards files stay on this list permanently, since settled decision 3 forbids editing them here and raises no upstream follow-up.
- [x] 10.2 Run the full AscendAgent build and verify it is green: `./gradlew build` and `./gradlew integrationTest` both succeed against the Floci Testcontainer.
- [x] 10.3 Run the full Python test suites and verify they are green: `pytest` in `AudioScribe/` and `pytest` in `PaddleOCR/`.
- [x] 10.4 Execute the rewritten RAG e2e group serially (5, then 6, then 7) against the live stack and verify all three report PASS, which is the end-to-end proof that ingestion, presigning, and de-duplication all work against Floci.
- [x] 10.5 Execute the rewritten PaddleOCR and AudioScribe object-store e2e tests and verify both report PASS.
- [x] 10.6 Verify the agent boots clean against a Floci with no `knowledge-base` bucket, repeating task 1.2 after every change has landed, so the startup bucket-creation path is proven on the final state of the code rather than only on the starting state.
