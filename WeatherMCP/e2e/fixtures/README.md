# e2e fixtures

WeatherMCP tools (`weather_current`, `weather_forecast`, `weather_historical`, `weather_air_quality`, `weather_geocode`)
take string arguments only — no file uploads. The current suite has no fixtures.

Reserved for future tests that might need canary content (e.g. recorded Open-Meteo responses for an air-gapped CI
profile via WireMock).
