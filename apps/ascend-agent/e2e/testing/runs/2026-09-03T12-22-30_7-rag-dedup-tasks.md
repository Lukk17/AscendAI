# RAG dedup: run tasks template

Spec: [../7-rag-dedup-test.md](../7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present: 3.4.0
- [x] AscendAgent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1`
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Removed `int_metadata_store` rows for both fixtures (0 rows, none existed)
- [x] Wiped Qdrant points for both fixtures
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest` (0 rows)
- [x] Deleted Redis key `chat:frostyRagDedupTest`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2`
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys
- [x] Step 2: `indexed >= 2`, `failed == 0` (indexed=4 chunk-count across the two new files, skipped=2 for the two already-indexed leftover objects from spec 6, failed=0)
- [x] Step 3: response `sources` array has exactly 2 entries
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order)
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract (H1 when extractable, filename basename otherwise): `"Grandma Maria's pierogi recipe (e2e dedup fixture)"` and `"Babcia Helena's pierogi recipe (e2e dedup fixture)"`
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings): content explicitly compares "Maria's Potato & Cheese Pierogi" and "Babcia Helena's Cabbage & Mushroom Pierogi" side by side

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the two dedup keys (2 rows)
- [x] Wiped Qdrant points for both `source` values in collection `ascendai-1536`
- [x] Truncated `chat_history` rows for `frostyRagDedupTest` (2 rows) + deleted Redis key `chat:frostyRagDedupTest`

### Verdict

- [x] Verdict: PASS

## Result summary

All Expected assertions held. Step 1 uploaded both dedup fixtures (HTTP 200, `uploaded` listed `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md`). Step 2 ingestion returned HTTP 200 with `indexed=4` (a chunk-level count: Qdrant `points/count` against the two dedup sources confirmed exactly 4 points, 2 chunks per file) and `skipped=2` (the two leftover objects from spec 6's fixture-sharing upload, `markdown-canary.md` and `banana-price-poland.pdf`, already indexed with unchanged ETags), `failed=0`. Step 3 sent the dedup prompt with `attachSources=true` and got HTTP 200 with a `sources` array of exactly 2 entries, proving `RagRetrievalService.buildSourceRefs` collapsed multiple Qdrant chunks into one entry per unique `(bucket, key)`. One entry's `downloadUrl` path contained `markdown/dedup-pierogi-helena.md` with `name="Babcia Helena's pierogi recipe (e2e dedup fixture)"`; the other's `downloadUrl` contained `markdown/dedup-pierogi-grandma.md` with `name="Grandma Maria's pierogi recipe (e2e dedup fixture)"`, both exactly matching each document's H1 title. The response `content` explicitly compared both recipes side by side (potato/cheese/twarog for Maria vs. sauerkraut/porcini for Helena), satisfying the soft content assertion. Post-run cleanup for the object store, `int_metadata_store`, Qdrant, chat_history, and Redis all completed with the expected status codes and row counts for this spec's own fixtures and user-id.

Input tokens: not available; the LLM-facing usage reported in the step 3 response body is `promptTokens=1426`, `completionTokens=1028` (model `claude-sonnet-4-6`, provider anthropic), but that is the tested system's own token accounting, not this runner's consumption, so it is left blank below.

Output tokens: not available (see note above).

Start (UTC): 2026-09-03T12:33:17Z

End (UTC): 2026-09-03T12:36:28Z

Duration: 00:03:11

---

## Additional tasks I did

- Confirmed via `curl -fsS "http://localhost:9070/knowledge-base?list-type=2"` after this spec's post-run cleanup that `documents/banana-price-poland.pdf` and `markdown/markdown-canary.md` are still present in the bucket. This is the same residual state flagged under spec 6's "Additional tasks I did": those two objects were written by spec 6's re-use of the shared 3-file upload request and are outside spec 7's Reset/cleanup scope (`dedup-pierogi-*` only), so this spec correctly did not touch them per its own hermetic contract. Recorded here so the caller has the full picture at the end of the Group A chain: the sweep leaves the `knowledge-base` bucket with 2 orphaned objects that no spec in this chain owns the cleanup for.
- Did not edit any Bruno `.yml` file for this spec; `rag-dedup-upload.yml`, `rag-ingestion-run.yml`, and `rag-dedup-prompt.yml` all ran unmodified.
