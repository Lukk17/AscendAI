# RAG dedup: run tasks template

Spec: [7-rag-dedup-test.md](7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present — 3.3.0
- [x] AscendAgent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] MinIO `/minio/health/live` returns 200
- [x] Postgres responds to `SELECT 1`
- [x] MinIO `mc` client present
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] Registered MinIO alias `local`
- [x] Dropped `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` from MinIO — objects did not exist (first run), which is expected
- [x] Removed `int_metadata_store` rows for both fixtures — DELETE 0 (no prior entries), expected
- [x] Wiped Qdrant points for both fixtures — acknowledged
- [x] Truncated `chat_history` rows for user `dedup-test`
- [x] Deleted Redis key `chat:dedup-test`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200 — uploaded: ["markdown/dedup-pierogi-helena.md","markdown/dedup-pierogi-grandma.md"]
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2` — indexed=4, skipped=3, failed=0
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys
- [x] Step 2: `indexed >= 2`, `failed == 0` — indexed=4 (4 chunks across 2 dedup files), failed=0
- [x] Step 3: response `sources` array has exactly 2 entries — FAIL: got 3 entries (pierogi-recipe.docx also retrieved because query topic overlaps)
- [ ] Step 3: `sources[*].name` includes both `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` — FAIL: name fields contain document titles ("Babcia Helena's pierogi recipe (e2e dedup fixture)", "Grandma Maria's pierogi recipe (e2e dedup fixture)") not filenames; also 3rd source `pierogi-recipe.docx` present
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings) — PASS: content explicitly compares "Babcia Helena" vs "Maria" with sauerkraut/mushroom vs potato/cheese fillings

### Verdict

- [x] Verdict: FAIL

## Result summary

Step 3 failed on two assertions: (1) `sources` array has 3 entries instead of exactly 2 — `pierogi-recipe.docx` from test 5/6 is still in Qdrant and was retrieved because the query "How do I make pierogi?" semantically matches all three pierogi documents; (2) `sources[*].name` fields contain human-readable document titles not the bare filenames the spec expects. The soft content assertion (LLM references both Helena and Maria recipes) passed.

Input tokens: N/A

Output tokens: N/A

Start (UTC): 2026-05-22T16:12:14Z

End (UTC): 2026-05-22T16:14:48Z

Duration: 00:02:34

---

## Additional tasks I did
