# RAG: run tasks template

Spec: [5-rag-test.md](5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version) — 3.4.0
- [x] AscendAgent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200 — `{"status":"ok"}`
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx — 422
- [x] Qdrant `/healthz` returns HTTP 200 — "healthz check passed"
- [x] MinIO `/minio/health/live` returns HTTP 200
- [x] Postgres responds to `SELECT 1` with a row
- [x] MinIO `mc` client present inside the `minio` container (`docker exec minio mc --version` returns a version)
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `AscendAgent/e2e/fixtures/`

### Reset state

- [x] Registered MinIO alias `local` inside the container (`docker exec minio sh -c 'mc alias set local ...'`)
- [x] Dropped all objects in MinIO for the three test fixtures (markdown-canary.md and banana-price-poland.pdf removed; pierogi-recipe.docx was already absent)
- [x] Truncated Postgres `int_metadata_store` rows for the three fixtures (DELETE 2 rows)
- [x] Wiped Qdrant `ascendai-1536` points for the three fixture sources — acknowledged

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3`
- [x] Step 3a: sent `rag-prompt.yml` for the markdown-canary prompt (toggled canary enabled, others disabled)
- [x] Step 3b: enabled banana-price row, sent `rag-prompt.yml`
- [x] Step 3c: enabled pierogi row (restored default), sent `rag-prompt.yml`

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`) — `["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"]`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/markdown/` lists `markdown-canary.md`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/documents/` lists both `banana-price-poland.pdf` and `pierogi-recipe.docx`
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 — `{"indexed":3,"skipped":2,"failed":0}`
- [x] Step 3a: response `content` contains the canary phrase — "The Ascend canary phrase is **PURPLE-MOOSE-42**."
- [x] Step 3b: response `content` contains a numeric price in PLN — "6.49 PLN per kilogram"
- [x] Step 3c: response `content` contains `30 minutes` / rest time — "exactly thirty minutes"
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document"

### Verdict

- [x] Verdict: PASS

## Result summary

All three fixtures uploaded, ingested (indexed=3, failed=0), and retrieved correctly via RAG prompts. Each prompt returned the expected grounded value from its source document.

Input tokens: n/a (Bruno CLI)

Output tokens: n/a (Bruno CLI)

Start (UTC): 2026-05-23T23:08:52Z

End (UTC): 2026-05-23T23:16:33Z

Duration: 00:07:41

---

## Additional tasks I did

- Toggled `disabled` flags in `rag-prompt.yml` for each of the three prompt rows; restored to original default (pierogi active) after step 3c.
