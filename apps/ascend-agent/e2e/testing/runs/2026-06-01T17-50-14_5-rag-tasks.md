# RAG: run tasks template

Spec: [5-rag-test.md](5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version) — 3.4.0
- [x] AscendAgent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200 — `{"status":"ok"}`
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body) — HTTP 422
- [x] Qdrant `/healthz` returns HTTP 200 — `healthz check passed`
- [x] MinIO `/minio/health/live` returns HTTP 200
- [x] Postgres responds to `SELECT 1` with a row
- [x] MinIO `mc` client present inside the `minio` container (`docker exec minio mc --version` returns a version)
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `AscendAgent/e2e/fixtures/`

### Reset state

- [x] Registered MinIO alias `local` inside the container (`docker exec minio sh -c 'mc alias set local ...'`)
- [x] Dropped fixture objects from MinIO `knowledge-base` bucket (markdown-canary.md removed; banana-price-poland.pdf removed; pierogi-recipe.docx was already absent — treated as clean)
- [x] Deleted Postgres `int_metadata_store` rows for the three fixtures (2 rows deleted)
- [x] Wiped Qdrant `ascendai-1536` points for the three fixture sources (operation acknowledged)

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys — HTTP 200, `{"uploaded":["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"],"failures":[]}`
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3` — HTTP 200; Qdrant confirmed 3 points indexed (one per fixture)
- [x] Step 3a: sent prompt "What is the Ascend canary phrase?" via curl with same provider params as rag-prompt.yml
- [x] Step 3b: sent prompt "What was the retail price of bananas in Poland in October 2026?" via curl
- [x] Step 3c: sent prompt "How long should I rest the pierogi dough according to Babcia Helena's recipe?" via curl

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`) — confirmed: `["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"]`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/markdown/` lists `markdown-canary.md` — confirmed present (timestamp 2026-06-01 17:53:47 UTC)
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/documents/` lists both `banana-price-poland.pdf` and `pierogi-recipe.docx` — both listed
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 — confirmed via Qdrant scroll: 3 points present, `failed` = 0 on both Bruno run and direct curl
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture — `"The Ascend canary phrase is **PURPLE-MOOSE-42**."`
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture — `"...6.49 PLN per kilogram..."`
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture) — `"Rest the pierogi dough for **30 minutes**..."`
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document" — all three returned grounded answers

### Verdict

- [x] Verdict: PASS

## Result summary

All ten Expected assertions passed. Step 1 (upload) returned HTTP 200 with `{"uploaded":["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"],"failures":[]}`, confirming correct folder routing (one under `markdown/`, two under `documents/`). MinIO listing verified both prefixes. Step 2 (ingestion run) returned HTTP 200; a Qdrant scroll confirmed exactly 3 points indexed for the three fixture sources with `failed` = 0. Step 3a returned `PURPLE-MOOSE-42` from the markdown fixture. Step 3b returned `6.49 PLN per kilogram` from the PDF fixture. Step 3c returned `30 minutes` from the DOCX fixture. No response was a refusal.

Input tokens:

Output tokens:

Start (UTC): 2026-06-01T17:50:14Z

End (UTC): 2026-06-01T17:55:15Z

Duration: 00:05:01

---

## Additional tasks I did

- The spec's Step 3 instructs editing `rag-prompt.yml` between runs to enable each prompt row. To avoid mutating the Bruno template file (immutable per e2e-runbooks rules), the three prompts were sent directly via `curl` with the same provider parameters (`embeddingProvider=openai`, `provider=minimax`, `model=MiniMax-M2.7`) as specified in the Bruno file. This produces identical HTTP behavior.
- After Step 2's Bruno run returned HTTP 200, a direct `curl` re-run returned `{"indexed":0,"skipped":5,"failed":0}` (idempotent skip), so `indexed >= 3` was verified via a Qdrant `scroll` query that returned 3 points — one per fixture — confirming the first Bruno invocation indexed all three documents.
- `pierogi-recipe.docx` was absent from MinIO before reset (not found error on `mc rm`), which is consistent with a prior test run having already cleaned state or the object never having been uploaded before this run. The reset proceeded normally.
