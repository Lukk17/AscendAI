# RAG dedup: run tasks template

Spec: [../7-rag-dedup-test.md](../7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present (3.4.0)
- [x] ascend-ai-agent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1`
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Removed `int_metadata_store` rows for both fixtures (DELETE 0, already clean)
- [x] Wiped Qdrant points for both fixtures (acknowledged, operation_id 431)
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest` (DELETE 0, already clean)
- [x] Deleted Redis key `chat:frostyRagDedupTest`
- [x] Deleted Redis key `user:frostyRagDedupTest:instructions`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2`
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys (Bruno test "Both dedup fixtures land under markdown/" passed)
- [x] Step 2: `indexed >= 2`, `failed == 0` (confirmed via persisted state: `int_metadata_store` has exactly 2 rows for the two dedup keys, Qdrant `ascendai-1536` has 4 points, 2 per source)
- [x] Step 3: response `sources` array has exactly 2 entries (Bruno test `expect(body.sources).to.be.an("array").with.lengthOf(2)` passed)
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order) (Bruno test asserted both `hasHelena` and `hasGrandma` true)
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract (Bruno test asserted `names` includes both exact H1 titles: "Babcia Helena's pierogi recipe (e2e dedup fixture)" and "Grandma Maria's pierogi recipe (e2e dedup fixture)")
- [x] Step 3 (soft): response `content` references both recipes (confirmed via persisted `chat_history.content`: response compares "Maria's Recipe" (potato & cheese) vs "Helena's Recipe" (cabbage & mushroom) in a full side-by-side table)

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the two dedup keys (DELETE 2)
- [x] Wiped Qdrant points for both `source` values in collection `ascendai-1536` (acknowledged, operation_id 438)
- [x] Truncated `chat_history` rows for `frostyRagDedupTest` (DELETE 4) + deleted Redis key `chat:frostyRagDedupTest`
- [x] Deleted Redis key `user:frostyRagDedupTest:instructions`
- [x] `POST http://localhost:7020/api/v1/memory/wipe?user_id=frostyRagDedupTest` returned `{"status":"success","message":"All memories wiped for user frostyRagDedupTest"}`

Verified clean: `int_metadata_store` count 0, `chat_history` count 0, Redis keys EXISTS 0/0, Qdrant scroll returns 0 points, both fixture objects 404.

### Verdict

- [x] Verdict: PASS

## Result summary

All three Run steps returned HTTP 200. Upload placed both dedup fixtures under `markdown/`. Ingestion indexed exactly 2 new objects (confirmed via `int_metadata_store`: 2 rows; Qdrant `ascendai-1536`: 4 points, 2 chunks per source), no failures. The dedup prompt's `sources[]` array collapsed those 4 chunks to exactly 2 entries, one per unique file, with `downloadUrl` correctly pointing at `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` respectively, and each `name` carrying the exact H1 title (Bruno's own assertions verified this, checked stronger than the spec requires — exact string match rather than the spec's looser "H1 when extractable" contract). The soft content assertion held: the persisted assistant response compares Maria's potato-and-cheese filling against Helena's cabbage-and-mushroom filling side by side. `RagRetrievalService.buildSourceRefs` dedup-by-`(bucket,key)` is proven. Post-run cleanup verified empty: 0 metadata rows, 0 chat_history rows, 0 Qdrant points, both fixture objects 404, Redis keys absent, memory wipe returned success.

Input tokens: not available (LLM API token counts are not exposed to this runner; Bruno CLI reports HTTP timing only, not model usage)

Output tokens: not available (same reason)

Start (UTC): 2026-09-10T07:53:45Z

End (UTC): 2026-09-10T07:58:10Z

Duration: 00:04:25

---

## Additional tasks I did

- Re-ran `rag-ingestion-run.yml` a second time (attempting to capture JSON output for evidence) after the first pass already passed. This second call returned `indexed: 0` because ingestion is idempotent by ETag and nothing new existed — confirms idempotency but was not prescribed by the spec's single Step 2 invocation.
- Re-ran `rag-dedup-prompt.yml` a second time (attempting `--output`/`--format json` to inspect the raw response body for the soft content assertion; the `--output` flag failed with a Windows `EPERM` writing outside the scratchpad, but the live request itself still executed and got billed/persisted). Confirmed via `chat_history` row count (4 rows: 2 user + 2 assistant turns) that both invocations landed. Both invocations passed the Bruno test script identically; no divergent behavior observed. Post-run cleanup removed all resulting rows regardless.
- Queried `int_metadata_store`, Qdrant `scroll`, and `chat_history` directly via `docker exec postgres psql` / `curl` to get concrete evidence values (row counts, point counts, response content) beyond what Bruno's pass/fail summary alone shows, since the harness asked for observed evidence per assertion.
- Compared the Bruno request `rag-ingestion-run.yml`'s own embedded test script against the spec: the script asserts `indexed >= 1`, weaker than the spec's Expected `indexed >= 2`. Confirmed via direct Postgres/Qdrant inspection that the actual run satisfied the stricter spec threshold (`indexed == 2`), so the weaker script check did not mask a failure this run, but it would not have caught a "only 1 of 2 fixtures indexed" defect on its own.
- Deleted my own scratchpad temp file (`assistant-response.txt`) after use; did not touch other pre-existing files in the scratchpad directory.
