# RAG: run tasks template

Spec: [5-rag-test.md](5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version) — 3.4.0
- [x] AscendAgent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200 — `{"status":"ok"}`
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body) — HTTP 422
- [x] Qdrant `/healthz` returns HTTP 200 — "healthz check passed"
- [x] MinIO `/minio/health/live` returns HTTP 200
- [x] Postgres responds to `SELECT 1` with a row
- [x] MinIO `mc` client present inside the `minio` container (`docker exec minio mc --version` returns a version) — RELEASE.2025-08-13
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `AscendAgent/e2e/fixtures/`

### Reset state

- [x] Registered MinIO alias `local` inside the container (`docker exec minio sh -c 'mc alias set local ...'`)
- [x] Dropped all objects in MinIO `knowledge-base` bucket — removed markdown-canary.md and banana-price-poland.pdf; pierogi-recipe.docx was already absent
- [x] Truncated Postgres `int_metadata_store` table — DELETE 2 rows for the three fixture keys
- [x] Wiped Qdrant `ascendai-*` points for the three fixture sources — operation acknowledged

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3`
- [x] Step 3a: sent `rag-prompt.yml` for the markdown-canary prompt
- [x] Step 3b: enabled the next `prompt=` row, sent `rag-prompt.yml` for the banana-price prompt
- [x] Step 3c: enabled the next `prompt=` row, sent `rag-prompt.yml` for the pierogi-recipe prompt

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`) — `["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"]`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/markdown/` lists `markdown-canary.md`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/documents/` lists both `banana-price-poland.pdf` and `pierogi-recipe.docx`
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 — 3 Qdrant points confirmed (1 per fixture); bru run took 40s indicating real indexing; failed=0
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture — "The Ascend canary phrase is **PURPLE-MOOSE-42**."
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture — "6.49 PLN per kilogram"
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture) — "exactly 30 minutes"
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document"

### Verdict

- [x] Verdict: PASS

## Result summary

All three fixtures uploaded, ingested, and retrieved correctly. Each RAG prompt returned the expected grounded answer from the correct source document. No refusals observed. Provider: MiniMax-M2.7.

Input tokens: 267 / 361 / 249 (canary / banana / pierogi)

Output tokens: 43 / 63 / 121

Start (UTC): 2026-05-24T01:48:03Z

End (UTC): 2026-05-24T02:02:00Z

Duration: 00:13:57

---

## Additional tasks I did

- Issued direct curl after each bru run to capture response body for assertion verification.
- Verified MinIO object listing after Step 1 upload.
- Confirmed Qdrant point counts (3 points, 1 per fixture) after Step 2 ingestion.
- Restored `rag-prompt.yml` to its original default state (pierogi enabled, canary/banana disabled) after all three sub-runs completed.
