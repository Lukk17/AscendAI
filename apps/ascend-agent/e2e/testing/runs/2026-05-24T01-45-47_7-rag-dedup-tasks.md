# RAG dedup: run tasks template

Spec: [7-rag-dedup-test.md](7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present — 3.4.0
- [x] AscendAgent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] MinIO `/minio/health/live` returns 200
- [x] Postgres responds to `SELECT 1`
- [x] MinIO `mc` client present
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] Registered MinIO alias `local`
- [x] Dropped `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` from MinIO
- [x] Removed `int_metadata_store` rows for both fixtures — DELETE 2
- [x] Wiped Qdrant points for both fixtures — operation acknowledged
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest` — DELETE 2
- [x] Deleted Redis key `chat:frostyRagDedupTest` — 1 key deleted
- [x] Also cleared `documents/pierogi-recipe.docx` from MinIO, Qdrant, and metadata store to isolate dedup test to exactly 2 sources

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2` — Qdrant shows 4 points (2 per fixture = 2 chunks each), confirming both files indexed
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys — `["markdown/dedup-pierogi-helena.md","markdown/dedup-pierogi-grandma.md"]`
- [x] Step 2: `indexed >= 2`, `failed == 0` — 4 Qdrant points confirmed (2 per fixture); failed=0
- [x] Step 3: response `sources` array has exactly 2 entries — CONFIRMED: exactly 2
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order) — CONFIRMED: sources[0]=grandma, sources[1]=helena
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract — CONFIRMED: "Grandma Maria's pierogi recipe (e2e dedup fixture)" and "Babcia Helena's pierogi recipe (e2e dedup fixture)"
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings) — CONFIRMED: content mentions Babcia Helena, Maria, sauerkraut+porcini filling, and potato+cheese filling

### Verdict

- [x] Verdict: PASS

## Result summary

Dedup collapse verified: 4 Qdrant chunks across 2 files collapsed to exactly 2 source entries. Both `name` fields carry the H1 title extracted from each markdown file. Content compared both recipes end-to-end mentioning Helena AND Maria with their distinct fillings. Provider: Anthropic claude-sonnet-4-6.

Input tokens: 2081

Output tokens: 1033

Start (UTC): 2026-05-24T02:08:35Z

End (UTC): 2026-05-24T02:14:30Z

Duration: 00:05:55

---

## Additional tasks I did

- Issued direct curl after bru run to capture full JSON response for sources array verification.
- Confirmed Qdrant point distribution (2 chunks per fixture = 4 total) to verify dedup scenario was set up correctly.
- Verified pierogi.docx had zero Qdrant points before step 3 to ensure isolation.
