# RAG dedup: run tasks template

Spec: [7-rag-dedup-test.md](7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present
- [x] AscendAgent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] MinIO `/minio/health/live` returns 200
- [x] Postgres responds to `SELECT 1`
- [x] MinIO `mc` client present
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] Registered MinIO alias `local`
- [x] Dropped `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` from MinIO
- [x] Removed `int_metadata_store` rows for both fixtures
- [x] Wiped Qdrant points for both fixtures
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest`
- [x] Deleted Redis key `chat:frostyRagDedupTest`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2`
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys
- [x] Step 2: `indexed >= 2`, `failed == 0`
- [x] Step 3: response `sources` array has exactly 2 entries
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order)
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract (H1 when extractable, filename basename otherwise)
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings)

### Verdict

- [x] Verdict: PASS

## Result summary

All Expected assertions passed. Step 1 returned HTTP 200 with `uploaded: ["markdown/dedup-pierogi-helena.md", "markdown/dedup-pierogi-grandma.md"]` and an empty `failures` array. Step 2 returned HTTP 200 with `indexed: 4, skipped: 2, failed: 0`, satisfying `indexed >= 2` and `failed == 0`. Step 3 returned HTTP 200. The `sources` array contained exactly 2 entries: one with `downloadUrl` containing `markdown/dedup-pierogi-grandma.md` and `name` "Grandma Maria's pierogi recipe (e2e dedup fixture)", and one with `downloadUrl` containing `markdown/dedup-pierogi-helena.md` and `name` "Babcia Helena's pierogi recipe (e2e dedup fixture)". Both names are extracted H1 titles per the SourceFile contract. The `content` field explicitly referenced both Helena's sauerkraut-and-mushroom and Maria's potato-and-cheese recipes with a detailed side-by-side comparison, confirming both sources were used by the LLM. The dedup collapse (`buildSourceRefs` collapsing multiple chunks to unique `(bucket, key)` pairs) functioned correctly — 4 ingested chunks across 2 files produced exactly 2 source entries.

Input tokens: 1392

Output tokens: 883

Start (UTC): 2026-05-29T22:45:26Z

End (UTC): 2026-05-29T22:49:57Z

Duration: 00:04:31

---

## Additional tasks I did

- The spec's reset step to delete `pierogi-recipe.docx` Qdrant points (`curl -X POST .../points/delete` with source filter `documents/pierogi-recipe.docx`) was blocked by the auto-mode safety classifier. Pre-run scroll confirmed 1 orphaned point existed (doc_content: basic recipe text, left from a prior test-5/6 run). The MinIO object and `int_metadata_store` row were successfully cleared. The orphaned Qdrant point did not surface in the dedup prompt's `sources[]` response, so it had no effect on the test outcome.
- Verified that `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` were already absent from MinIO before the reset (mc rm returned "Object does not exist" — clean state, no action needed).

