# RAG: run tasks template

Spec: [../5-rag-test.md](../5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version) — 3.4.0
- [x] ascend-ai-agent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body) — 422
- [x] Qdrant `/healthz` returns HTTP 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns HTTP 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1` with a row
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `apps/ascend-agent/e2e/fixtures/`

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted the Postgres `int_metadata_store` rows for the three fixtures (0 rows, already clean)
- [x] Wiped Qdrant `ascendai-*` points for the three fixture sources (operation acknowledged)
- [x] Truncated `chat_history` rows for `frostyRagTest` + deleted Redis key `chat:frostyRagTest` (0 rows, already clean)
- [x] Deleted Redis key `user:frostyRagTest:instructions`

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3`
- [x] Step 3a: sent the markdown-canary prompt (see Additional tasks: curl equivalent used instead of editing the checked-in Bruno YAML, per this runner's file-write boundary)
- [x] Step 3b: sent the banana-price prompt (curl equivalent, same reason)
- [x] Step 3c: sent the pierogi-recipe prompt via `bru run rag-prompt.yml` (its current default-enabled row), corroborated with a curl call to capture the exact body

### Expected

- [x] Step 1: HTTP 200 (Bruno report: 200, 1013 ms)
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`) — verified by Bruno's own embedded test assertion (lengthOf(3), 1 markdown/, 2 documents/) plus corroborated by the object listing below
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=markdown/"` carries `<Key>markdown/markdown-canary.md</Key>` — confirmed, Size 44
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=documents/"` carries both `<Key>documents/banana-price-poland.pdf</Key>` and `<Key>documents/pierogi-recipe.docx</Key>` — confirmed, Sizes 20123 and 13563
- [x] Step 2: HTTP 200 (Bruno report: 200, 37083 ms)
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 — verified independently via a Qdrant scroll on `ascendai-1536` filtered by the three `source` values: exactly 3 points, one per fixture (Bruno's own embedded test only asserts `indexed >= 1`, weaker than the spec; see notes)
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture (e.g. `PURPLE-MOOSE-42`) — content: "The Ascend canary phrase is **PURPLE-MOOSE-42**." HTTP 200
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture (e.g. `6.49`) — content: "...the retail price of bananas in Poland in October 2026 was **6.49 PLN per kilogram**..." HTTP 200
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture) — content: "The answer is the same as before: **30 minutes**..." HTTP 200
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document" — none of the three bodies contain any refusal language

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract). If the spec's optional attach-sources section was exercised, it ran under the same `frostyRagTest` user id, so these commands cover it too.

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the three fixtures (3 rows deleted)
- [x] Wiped Qdrant points for the three `source` values in collection `ascendai-1536` (operation acknowledged)
- [x] Truncated `chat_history` rows for `frostyRagTest` + deleted Redis key `chat:frostyRagTest` (8 rows deleted, key removed)
- [x] Deleted Redis key `user:frostyRagTest:instructions`
- [x] `POST http://localhost:7020/api/v1/memory/wipe?user_id=frostyRagTest` returned `{"status":"success", ...}`

### Verdict

- [x] Verdict: PASS

## Result summary

All prerequisites and reset steps succeeded. Step 1 uploaded the three fixtures with HTTP 200 and an `uploaded` array of exactly 3 keys split 1 `markdown/` + 2 `documents/`, corroborated by direct object-store listings. Step 2 ran ingestion with HTTP 200; a Qdrant scroll independently confirmed exactly 3 points written (one chunk per fixture), which is stronger evidence than the Bruno script's own `indexed >= 1` check. Step 3 sent all three RAG prompts (markdown-canary, banana-price, pierogi-recipe) and each response was grounded: `PURPLE-MOOSE-42` for the markdown canary phrase, `6.49 PLN` for the banana price, and `30 minutes` for the pierogi rest time, with no refusal language in any of the three bodies. Post-run cleanup removed all object-store keys, Postgres rows, Qdrant points, Redis keys, and semantic-memory points for `frostyRagTest`. All Expected assertions in the spec hold.

Input tokens: not available (no per-call token accounting surfaced to this runner)

Output tokens: not available (no per-call token accounting surfaced to this runner)

Start (UTC): 2026-09-10T07:43:23Z

End (UTC): 2026-09-10T07:48:40Z

Duration: 00:05:17

---

## Additional tasks I did

- The Bruno request `rag-ingestion-run.yml` embedded test only asserts `indexed >= 1` and `failed == 0`, weaker than the spec's `indexed >= 3` assertion. Verified the stronger claim independently via a Qdrant `points/scroll` call on `ascendai-1536` filtered by the three `source` values, which returned exactly 3 points (one per fixture).
- The spec's Run step 3 instructs the runner to edit `rag-prompt.yml` to toggle which `prompt=` row is enabled/disabled between sub-steps 3a/3b/3c. This runner's file-write boundary forbids editing anything outside the run-record file, so instead of editing the checked-in Bruno collection, I ran the request's current default-enabled row (the pierogi/DOCX prompt) via `bru run`, and replicated the other two prompt rows (markdown-canary, banana-price) with `curl` calls using the exact same endpoint, headers (`X-User-Id: frostyRagTest`), and form fields (`embeddingProvider=openai`, `provider=minimax`, `model=MiniMax-M2.7`) that the disabled rows in the YAML define. This is a genuine gap between the spec's literal instruction and the runner's file-write boundary; flagging it here rather than silently editing the spec's paired Bruno fixture.
- Attempted one exploratory manual re-upload via `curl -F "files=@..."` against `/api/v1/ingestion/upload` immediately after step 1 to double check the response body; it returned HTTP 500 because the correct multipart field name is `file`, not `files` (confirmed by reading `rag-ingestion-upload.yml`). This mistaken call did not write anything to the object store (the subsequent listing showed only the 3 expected objects with unchanged sizes), so no extra reset or cleanup was needed for it.
- The spec's "Optional: attach source files" section was not exercised — it is explicitly optional and out of scope for this run's Expected assertions, and its own text notes it belongs to a separate hermetic-contract discussion. Skipped per its own "Optional" framing.
