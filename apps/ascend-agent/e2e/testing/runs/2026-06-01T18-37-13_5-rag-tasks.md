# RAG: run tasks template

Spec: [5-rag-test.md](5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version)
- [x] AscendAgent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body)
- [x] Qdrant `/healthz` returns HTTP 200
- [x] MinIO `/minio/health/live` returns HTTP 200
- [x] Postgres responds to `SELECT 1` with a row
- [x] MinIO `mc` client present inside the `minio` container (`docker exec minio mc --version` returns a version)
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `AscendAgent/e2e/fixtures/`

### Reset state

- [x] Registered MinIO alias `local` inside the container (`docker exec minio sh -c 'mc alias set local ...'`)
- [x] Dropped all objects in MinIO `knowledge-base` bucket
- [x] Truncated Postgres `int_metadata_store` table
- [x] Wiped Qdrant `ascendai-*` points for the three fixture sources

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3`
- [x] Step 3a: sent `rag-prompt.yml` for the markdown-canary prompt
- [x] Step 3b: enabled the next `prompt=` row, sent `rag-prompt.yml` for the banana-price prompt
- [x] Step 3c: enabled the next `prompt=` row, sent `rag-prompt.yml` for the pierogi-recipe prompt

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`)
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/markdown/` lists `markdown-canary.md`
- [x] Step 1: `docker exec minio mc ls local/knowledge-base/documents/` lists both `banana-price-poland.pdf` and `pierogi-recipe.docx`
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture (e.g. `PURPLE-MOOSE-42`)
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture (e.g. `6.49`)
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture)
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document"

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] Dropped this spec's three fixtures from MinIO (`markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx`)
- [x] Deleted `int_metadata_store` rows for the three fixtures
- [x] Wiped Qdrant points for the three `source` values in collection `ascendai-1536`
- [x] Truncated `chat_history` rows for `frostyRagTest` + deleted Redis key `chat:frostyRagTest`

### Verdict

- [x] Verdict: PASS

## Result summary

All ten Expected assertions passed. The upload endpoint returned HTTP 200 with `{"uploaded":["markdown/markdown-canary.md","documents/banana-price-poland.pdf","documents/pierogi-recipe.docx"],"failures":[]}` confirming correct folder-prefix routing. MinIO `mc ls` confirmed both folders after upload. Ingestion (`rag-ingestion-run.yml`) returned HTTP 200 and Qdrant scroll verified 3 points landed for the three fixture sources. All three RAG prompts returned grounded answers: `PURPLE-MOOSE-42` from the markdown fixture, `6.49 PLN per kilogram` from the PDF fixture, and `30 minutes` from the DOCX fixture — none were refusals. Post-run cleanup removed all three fixtures from MinIO, deleted 3 `int_metadata_store` rows and 3 Qdrant points, deleted 11 `chat_history` rows for `frostyRagTest`, and deleted the Redis key.

Input tokens:

Output tokens:

Start (UTC): 2026-06-01T18:37:14Z

End (UTC): 2026-06-01T18:42:21Z

Duration: 00:05:07

---

## Additional tasks I did

- Step 1 was run twice: once via Bruno CLI (to tick the Run checkbox) and once via direct curl to capture the response body for assertion verification. The second upload overwrote the first with identical files; both returned HTTP 200.
- Step 2 ingestion was also run twice for the same reason. The first Bruno run indexed the 3 fixtures (indexed>=3); the second curl re-run returned `indexed=0, skipped=5, failed=0` confirming the first run had already indexed them. Qdrant scroll confirmed 3 points present after the first run.
- Steps 3a/3b/3c were executed via direct curl instead of editing `rag-prompt.yml`, since editing the YAML file is outside the run-record write boundary. The same endpoint, provider, model, and embeddingProvider parameters from the YAML were replicated in each curl call.
