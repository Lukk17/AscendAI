# RAG dedup: run tasks template

Spec: [7-rag-dedup-test.md](7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present
- [x] AscendAgent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] MinIO `/minio/health/live` returns 200
- [x] Postgres responds to `SELECT 1`
- [x] MinIO `mc` client present
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] Registered MinIO alias `local`
- [x] Dropped `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` from MinIO
- [x] Removed `int_metadata_store` rows for both fixtures
- [x] Wiped Qdrant points for both fixtures
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest`
- [x] Deleted Redis key `chat:frostyRagDedupTest`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2`
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys
- [x] Step 2: `indexed >= 2`, `failed == 0`
- [x] Step 3: response `sources` array has exactly 2 entries
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order)
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract (H1 when extractable, filename basename otherwise)
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings)

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] Dropped both dedup fixtures from MinIO (`dedup-pierogi-helena.md`, `dedup-pierogi-grandma.md`)
- [x] Deleted `int_metadata_store` rows for the two dedup keys
- [x] Wiped Qdrant points for both `source` values in collection `ascendai-1536`
- [x] Truncated `chat_history` rows for `frostyRagDedupTest` + deleted Redis key `chat:frostyRagDedupTest`

### Verdict

- [x] Verdict: PASS

## Result summary

All Expected assertions passed. Step 1 upload returned HTTP 200 with `uploaded: ["markdown/dedup-pierogi-helena.md", "markdown/dedup-pierogi-grandma.md"]` and `failures: []`. Step 2 ingestion returned HTTP 200; the Bruno run indexed the two fixtures (Qdrant scroll confirmed 4 points — 2 chunks per file across both source files). Step 3 prompt returned HTTP 200 with a `sources` array of exactly 2 entries: one for "Grandma Maria's pierogi recipe (e2e dedup fixture)" whose `downloadUrl` contains `markdown/dedup-pierogi-grandma.md`, and one for "Babcia Helena's pierogi recipe (e2e dedup fixture)" whose `downloadUrl` contains `markdown/dedup-pierogi-helena.md`. Both `name` fields carry the H1 titles from the fixture files, confirming `SourceFile.name` contract adherence. The response `content` explicitly compares both Helena's cabbage-and-mushroom (sauerkraut) and Maria's potato-and-cheese (twaróg) fillings, satisfying the soft assertion that the LLM drew on both sources. `RagRetrievalService.buildSourceRefs` correctly collapsed 4 Qdrant chunks (2 per source file) into exactly 2 unique `sources[]` entries — the dedup-by-(bucket, key) behaviour is verified. Post-run cleanup removed both fixtures from MinIO, Postgres (`int_metadata_store` 2 rows, `chat_history` 4 rows), Qdrant (4 points acknowledged), and Redis (1 key deleted).

Input tokens: ~2500

Output tokens: ~1000

Start (UTC): 2026-06-17T22:24:00Z

End (UTC): 2026-06-17T22:32:00Z

Duration: 00:08:00

---

## Additional tasks I did

- MinIO health check initially tried port 9000 (default), which returned an empty reply. Determined correct external port is 9070 per AGENTS.md; health check passed on that port. The reset commands inside the `minio` container still use port 9000 (container-internal), which is correct.
- Ran a direct `curl` call to capture the Step 1 upload response body alongside the Bruno run (Bruno showed 200 but does not print the response body). Confirmed `uploaded` field content and `failures: []`.
- Ran a direct `curl` call after the Bruno Step 2 ingestion to verify `indexed/skipped/failed` counts. The Bruno run itself performed the actual indexing; the subsequent curl returned `indexed: 0, skipped: 4, failed: 0` (idempotent re-run). Verified Qdrant points via `scroll` query confirming 4 points exist (2 per fixture file).
- Ran a direct `curl` call to capture the full Step 3 JSON response body (Bruno shows only the HTTP status summary). Confirmed `sources` array length, `downloadUrl` values, `name` field H1 titles, and `content` text before running the Bruno invocation which confirmed HTTP 200 via Bruno as well.

