# RAG: run tasks template

Spec: [../5-rag-test.md](../5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version) — 3.4.0
- [x] ascend-ai-agent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body) — 422
- [x] Qdrant `/healthz` returns HTTP 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns HTTP 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1` with a row
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `apps/ascend-agent/e2e/fixtures/`

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted the Postgres `int_metadata_store` rows for the three fixtures — DELETE 2
- [x] Wiped Qdrant `ascendai-*` points for the three fixture sources — acknowledged
- [x] Truncated `chat_history` rows for `frostyRagTest` + deleted Redis key `chat:frostyRagTest`
- [x] Deleted Redis key `user:frostyRagTest:instructions`

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3` — indexed:3, skipped:2, failed:0
- [x] Step 3a: sent the markdown-canary prompt via `curl` mirroring `rag-prompt.yml`'s body fields (see Additional tasks I did)
- [x] Step 3b: sent the banana-price prompt via `curl` mirroring `rag-prompt.yml`'s body fields
- [x] Step 3c: sent the pierogi-recipe prompt via `curl` mirroring `rag-prompt.yml`'s body fields

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`) — Bruno test "All three fixtures land under the right folder prefix" passed
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=markdown/"` carries `<Key>markdown/markdown-canary.md</Key>`
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=documents/"` carries both `<Key>documents/banana-price-poland.pdf</Key>` and `<Key>documents/pierogi-recipe.docx</Key>`
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 — captured raw body `{"indexed":3,"skipped":2,"failed":0}`
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture (e.g. `PURPLE-MOOSE-42`) — "The Ascend canary phrase is **PURPLE-MOOSE-42**."
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture (e.g. `6.49`) — "**6.49 PLN per kilogram**"
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture) — "**exactly 30 minutes**"
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document"

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract). If the spec's optional attach-sources section was exercised, it ran under the same `frostyRagTest` user id, so these commands cover it too.

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the three fixtures — DELETE 3
- [x] Wiped Qdrant points for the three `source` values in collection `ascendai-1536` — acknowledged
- [x] Truncated `chat_history` rows for `frostyRagTest` + deleted Redis key `chat:frostyRagTest` — DELETE 6 rows
- [x] Deleted Redis key `user:frostyRagTest:instructions`
- [x] `POST http://localhost:7020/api/v1/memory/wipe?user_id=frostyRagTest` returned `{"status":"success", ...}`

### Verdict

- [x] Verdict: PASS

## Result summary

All Expected assertions verified. Step 1 uploaded the three fixtures (HTTP 200), Bruno confirmed each landed under the correct prefix, and object-store listings independently confirmed `markdown/markdown-canary.md`, `documents/banana-price-poland.pdf`, and `documents/pierogi-recipe.docx`. Step 2 triggered ingestion (HTTP 200) and the raw response body showed `indexed:3, failed:0` (2 skipped were pre-existing test-7 territory fixtures already indexed, unrelated to this spec). Step 3a/3b/3c each returned grounded, non-refusal answers: the markdown canary phrase `PURPLE-MOOSE-42`, the PDF price `6.49 PLN`, and the DOCX rest time `30 minutes`. Post-run cleanup removed all object-store keys, Postgres rows, Qdrant points, Redis keys, and semantic-memory points this spec owns.

Input tokens: ~26000 (estimate, incl. tool outputs and file reads)

Output tokens: ~7000 (estimate)

Start (UTC): 2026-09-09T17:25:08Z

End (UTC): 2026-09-09T17:30:35Z

Duration: 00:05:27

---

## Additional tasks I did

Step 3 (a/b/c) required cycling three `prompt=` rows in `docs/api/request/AscendAI/ascend-agent/testing/rag-prompt.yml` by editing the YAML, per the spec's literal instructions. My file-write boundary as e2e-runner restricts writes to only the one run-record file, so I did not edit that Bruno request file. Instead I sent the three prompts via `curl` against `POST /api/v1/ai/prompt`, reproducing the exact form fields (`prompt`, `embeddingProvider=openai`, `provider=minimax`, `model=MiniMax-M2.7`) and header (`X-User-Id: frostyRagTest`) that `rag-prompt.yml` would have sent for each row. Same endpoint, same observable behaviour, no edit to a file outside my boundary. Flagging this for the caller in case the Bruno-only invocation is a hard requirement rather than an interchangeable API-client detail.

Step 2's `indexed` count needed to be read from the raw response body to check the spec's `indexed >= 3` assertion (the Bruno test itself only asserts `indexed >= 1`, a looser bound than the spec). Bruno's default terminal output does not print the response body, so after the first `bru run` (whose result I could not re-observe once ingestion made the fixtures idempotent-skipped), I re-deleted the three fixtures' `int_metadata_store` rows (already within this spec's Reset-state write scope) and re-ran ingestion with `-o` to capture a JSON transcript into the scratchpad directory, confirmed `indexed:3, failed:0`, then deleted the scratch file.

The spec's Optional "attach source files" section (lines 249-297) was not exercised. It is not mirrored in the paired tasks-template's checklist, and it duplicates capability that spec 6 (`6-attach-sources-test.md`) owns end to end, so I treated it as out of scope for this run rather than inventing template checkboxes for it.

No prerequisite, reset, or post-run cleanup command was blocked by the permission classifier during this run; the allowlist in `.claude/settings.local.json` covered every command issued.
