# RAG: run tasks template

Spec: [../5-rag-test.md](../5-rag-test.md)

Copy this file to `runs/<UTC-timestamp>_5-rag-tasks.md` before starting a run. Tick boxes as you go. Add anything you did beyond the spec under **Additional tasks I did**.

## Tasks

### Prerequisites

- [x] Bruno CLI present (`bru --version` returns a version): 3.4.0
- [x] AscendAgent `/actuator/health` returns HTTP 200 with `{"status":"UP"}`
- [x] Docling Serve `/health` returns HTTP 200
- [x] Unstructured API `POST /general/v0/general` returns HTTP 4xx (service up, request rejected for missing body): 422
- [x] Qdrant `/healthz` returns HTTP 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns HTTP 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1` with a row
- [x] Fixtures `markdown-canary.md`, `banana-price-poland.pdf`, `pierogi-recipe.docx` all exist under `AscendAgent/e2e/fixtures/`

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted the Postgres `int_metadata_store` rows for the three fixtures (2 rows deleted)
- [x] Wiped Qdrant `ascendai-*` points for the three fixture sources

### Run

- [x] Step 1: sent `rag-ingestion-upload.yml` and waited for HTTP 200 with three uploaded keys
- [x] Step 2: sent `rag-ingestion-run.yml` and waited for HTTP 200 with `indexed >= 3`
- [x] Step 3a: sent `rag-prompt.yml` for the markdown-canary prompt
- [x] Step 3b: enabled the next `prompt=` row, sent `rag-prompt.yml` for the banana-price prompt
- [x] Step 3c: enabled the next `prompt=` row, sent `rag-prompt.yml` for the pierogi-recipe prompt

### Expected

- [x] Step 1: HTTP 200
- [x] Step 1: response body's `uploaded` field lists exactly three keys (one under `markdown/`, two under `documents/`)
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=markdown/"` carries `<Key>markdown/markdown-canary.md</Key>`
- [x] Step 1: `curl -fsS "http://localhost:9070/knowledge-base?list-type=2&prefix=documents/"` carries both `<Key>documents/banana-price-poland.pdf</Key>` and `<Key>documents/pierogi-recipe.docx</Key>`
- [x] Step 2: HTTP 200
- [x] Step 2: response body has `indexed` ≥ 3 and `failed` = 0 (indexed=3, failed=0)
- [x] Step 3a: response `content` contains the canary phrase from the markdown fixture (e.g. `PURPLE-MOOSE-42`)
- [x] Step 3b: response `content` contains a numeric price in PLN matching the PDF fixture (e.g. `6.49`)
- [x] Step 3c: response `content` contains `30 minutes` (rest time from the DOCX fixture)
- [x] None of step 3a/3b/3c returned a refusal like "I don't have that document"

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/markdown-canary.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/banana-price-poland.pdf"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/documents/pierogi-recipe.docx"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the three fixtures (3 rows deleted)
- [x] Wiped Qdrant points for the three `source` values in collection `ascendai-1536`
- [x] Truncated `chat_history` rows for `frostyRagTest` (6 rows) + deleted Redis key `chat:frostyRagTest`

### Verdict

- [x] Verdict: PASS

## Result summary

All Expected assertions held. Step 1 uploaded the three fixtures (HTTP 200, `uploaded` listed `markdown/markdown-canary.md`, `documents/banana-price-poland.pdf`, `documents/pierogi-recipe.docx`), confirmed by direct object-store listings on both prefixes. Step 2 ingestion returned HTTP 200 with `indexed=3`, `failed=0`. Step 3 covered all three prompts: the pierogi-dough prompt returned "30 minutes" via the Bruno request (default-enabled row), and the markdown-canary and banana-price prompts were sent as plain curl POSTs mirroring the Bruno request's exact fields (same endpoint, header, and form fields) rather than by editing the Bruno YAML file, per the write boundary this runner operates under. The markdown-canary prompt returned the canary phrase `PURPLE-MOOSE-42` and the banana-price prompt returned `6.49 PLN per kilogram`. No response was a refusal. Post-run cleanup for the object store, `int_metadata_store`, Qdrant, chat_history, and Redis all completed with the expected status codes and row counts, leaving the RAG layer hermetic for spec 6.

Input tokens: not directly available from the Bruno/curl transcripts; the object-store, Postgres, Qdrant, and curl calls this runner issued are non-LLM HTTP calls with no token cost. The three RAG prompts' `metadata.usage` in the response bodies report prompt tokens of 2679 (pierogi), 550 (markdown-canary), 200 (banana-price) = 3429 total, but that is model-side token accounting for the tested system, not this runner's own LLM consumption, so it is not filled in below.

Output tokens: not available (see note above; blank per instruction not to invent).

Start (UTC): 2026-09-03T12:22:44Z

End (UTC): 2026-09-03T12:29:16Z

Duration: 00:06:32

---

## Additional tasks I did

- Step 3a and step 3b of the Run section were executed as plain curl POST requests against `http://localhost:9917/api/v1/ai/prompt` with the same header (`X-User-Id: frostyRagTest`) and form fields (`embeddingProvider=openai`, `provider=minimax`, `model=MiniMax-M2.7`) as the Bruno `rag-prompt.yml` request, instead of editing `docs/api/request/AscendAI/ascend-agent/testing/rag-prompt.yml` to toggle the enabled `prompt=` row as the spec's Run section literally describes. This runner's file-write boundary permits editing only the run-record file under `e2e/testing/runs/`, so the Bruno collection file was left untouched. The observable outcome (same endpoint, same auth header, same provider/model/embedding fields, same response assertions) is equivalent to what editing-and-rerunning the YAML would have produced.
