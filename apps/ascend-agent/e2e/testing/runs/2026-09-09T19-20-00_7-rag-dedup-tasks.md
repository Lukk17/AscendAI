# RAG dedup: run tasks template

Spec: [../7-rag-dedup-test.md](../7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present
- [x] ascend-ai-agent `/actuator/health` returns 200
- [x] Qdrant `/healthz` returns 200
- [x] Object store `curl -fsS http://localhost:9070/_floci/health` returns 200 with `"s3":"running"`
- [x] Postgres responds to `SELECT 1`
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Removed `int_metadata_store` rows for both fixtures
- [x] Wiped Qdrant points for both fixtures
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest`
- [x] Deleted Redis key `chat:frostyRagDedupTest`
- [x] Deleted Redis key `user:frostyRagDedupTest:instructions`

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2`
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys
- [x] Step 2: `indexed >= 2`, `failed == 0`
- [x] Step 3: response `sources` array has exactly 2 entries
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order)
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract (H1 when extractable, filename basename otherwise)
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings)

### Post-run cleanup

Run regardless of Run-step verdict (idempotent; honours Group A hermetic contract).

- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-helena.md"` returned HTTP 204
- [x] `curl -fsS -X DELETE "http://localhost:9070/knowledge-base/markdown/dedup-pierogi-grandma.md"` returned HTTP 204
- [x] Deleted `int_metadata_store` rows for the two dedup keys
- [x] Wiped Qdrant points for both `source` values in collection `ascendai-1536`
- [x] Truncated `chat_history` rows for `frostyRagDedupTest` + deleted Redis key `chat:frostyRagDedupTest`
- [x] Deleted Redis key `user:frostyRagDedupTest:instructions`
- [x] `POST http://localhost:7020/api/v1/memory/wipe?user_id=frostyRagDedupTest` returned `{"status":"success", ...}`

### Verdict

- [x] Verdict: PASS

## Result summary

All three Run steps returned HTTP 200. Step 1 uploaded both dedup fixtures to `markdown/`. Step 2 ingestion indexed them with no failures. Step 3's response `sources[]` array contained exactly 2 entries, proving `RagRetrievalService.buildSourceRefs` collapsed 4 retrieved Qdrant chunks (2 per file) down to one entry per unique source file. One entry's `downloadUrl` pointed at `markdown/dedup-pierogi-grandma.md`, the other at `markdown/dedup-pierogi-helena.md`. Each entry's `name` carried the document's H1 title verbatim ("Grandma Maria's pierogi recipe (e2e dedup fixture)" and "Babcia Helena's pierogi recipe (e2e dedup fixture)"), matching the `SourceFile.name` contract. The response `content` referenced both recipes in detail (Maria's potato-and-cheese filling and Helena's cabbage-and-mushroom filling), satisfying the soft assertion. Post-run cleanup verified clean: object store listing empty, `int_metadata_store` and `chat_history` row counts both 0, Qdrant scroll returned 0 points, both Redis keys absent.

Input tokens: 5985 (from the verifying `rag-dedup-prompt` call's `nativeUsage.input_tokens`, includes 3900 cache-read tokens)

Output tokens: 1229 (from `nativeUsage.output_tokens`)

Start (UTC): 2026-09-09T17:38:40Z

End (UTC): 2026-09-09T17:42:38Z

Duration: 00:03:58

---

## Additional tasks I did

- Before running Reset state, queried the pre-reset state of all four backing stores (object store listing, `int_metadata_store` row scan, Qdrant scroll, and Postgres) to confirm and record the leaked artifacts a prior sweep's run of this spec left behind, per the caller's explicit request. Found: both dedup fixtures present in the `knowledge-base` bucket (uploaded 2026-09-07T08:48:07Z), 2 matching `int_metadata_store` rows, and 4 Qdrant points (2 chunks each) across both `source` values. Reset state removed all of it before the Run steps began.
- Re-invoked `rag-dedup-prompt.yml` twice more beyond the one prescribed Run step 3 invocation, to capture the full JSON response body (Bruno's default console output truncates it) and directly inspect `sources[].downloadUrl`, `sources[].name`, and `content` against the spec's detailed Expected assertions rather than relying solely on the embedded test script's boolean pass. This added 2 extra prompt/response turns (4 chat_history rows) for `frostyRagDedupTest` and 2 extra background memory-extractor runs; all were wiped by Post-run cleanup along with the single prescribed invocation's rows (6 total `chat_history` rows deleted).
- After the third invocation, wrote its JSON response to a scratchpad file (`$SCRATCHPAD/dedup-prompt-result.json`) to read it, then deleted that file per the scratchpad-cleanup instruction.
- Ran a final post-cleanup verification pass (object store listing, Postgres counts, Qdrant scroll, Redis EXISTS checks) beyond what the spec's Post-run cleanup section itself prescribes, per the caller's explicit "make sure Post-run cleanup completes and leaves nothing behind" instruction. All four confirmed empty.
