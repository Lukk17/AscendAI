# RAG dedup: run tasks template

Spec: [7-rag-dedup-test.md](7-rag-dedup-test.md)

Copy this file to `runs/<UTC-timestamp>_7-rag-dedup-tasks.md` before starting a run. Tick boxes as you go.

## Tasks

### Prerequisites

- [x] Bruno CLI present — 3.4.0
- [x] AscendAgent `/actuator/health` returns 200 — `{"status":"UP","groups":["liveness","readiness"]}`
- [x] Qdrant `/healthz` returns 200 — `healthz check passed`
- [x] MinIO `/minio/health/live` returns 200 — (HTTP 200, empty body; MinIO is on port 9070 externally)
- [x] Postgres responds to `SELECT 1` — 1 row returned
- [x] MinIO `mc` client present — RELEASE.2025-08-13T08-35-41Z
- [x] Fixtures `dedup-pierogi-helena.md` and `dedup-pierogi-grandma.md` exist

### Reset state

- [x] Registered MinIO alias `local` — `Added 'local' successfully.`
- [x] Dropped `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` from MinIO — both already absent (Object does not exist errors treated as clean)
- [x] Removed `int_metadata_store` rows for both fixtures — DELETE 0 (rows already absent)
- [x] Wiped Qdrant points for both fixtures — `{"result":{"operation_id":217,"status":"acknowledged"}}`
- [x] Truncated `chat_history` rows for user `frostyRagDedupTest` — DELETE 2
- [x] Deleted Redis key `chat:frostyRagDedupTest` — key not present (0)
- NOTE: spec-prescribed `pierogi-recipe.docx` cleanup was PARTIALLY completed. Postgres metadata row was removed (DELETE 1). MinIO `mc rm` and Qdrant `points/delete` for this artifact were blocked by the auto-mode classifier. The Qdrant point for `documents/pierogi-recipe.docx` remained live, and the ingestion run re-indexed the DOCX (since metadata row was deleted), producing a 3rd source in Qdrant.

### Run

- [x] Step 1: sent `rag-dedup-upload.yml`, HTTP 200 — `{"uploaded":["markdown/dedup-pierogi-helena.md","markdown/dedup-pierogi-grandma.md"],"failures":[]}`
- [x] Step 2: sent `rag-ingestion-run.yml`, HTTP 200 with `indexed >= 2` — `{"indexed":5,"skipped":2,"failed":0}` (5 indexed: 2 dedup chunks each = 4, plus re-indexed pierogi-recipe.docx = 1 more due to incomplete reset)
- [x] Step 3: sent `rag-dedup-prompt.yml`, HTTP 200

### Expected

- [x] Step 1: response `uploaded` includes both dedup fixture keys — confirmed: `["markdown/dedup-pierogi-helena.md","markdown/dedup-pierogi-grandma.md"]`
- [x] Step 2: `indexed >= 2`, `failed == 0` — `indexed: 5, failed: 0`
- [ ] Step 3: response `sources` array has exactly 2 entries — FAILED: 3 entries returned (sources[0]=Grandma Maria's, sources[1]=Babcia Helena's, sources[2]=pierogi-recipe.docx)
- [x] Step 3: one entry's `downloadUrl` contains `markdown/dedup-pierogi-helena.md`; the other's contains `markdown/dedup-pierogi-grandma.md` (in either order) — both present in sources
- [x] Step 3: each entry's `name` carries the document's H1 title per the `SourceFile.name` contract — `"Grandma Maria's pierogi recipe (e2e dedup fixture)"` and `"Babcia Helena's pierogi recipe (e2e dedup fixture)"` confirmed (basename fallback for DOCX: `"pierogi-recipe.docx"`)
- [x] Step 3 (soft): response `content` references both recipes (Helena/Maria, or sauerkraut/potato fillings) — content mentions both Babcia Helena (cabbage & mushroom) and Maria (potato & cheese)

### Verdict

- [ ] Verdict: FAIL

## Result summary

Five of six Expected assertions passed. Steps 1 and 2 both returned HTTP 200 — upload confirmed both `markdown/dedup-pierogi-helena.md` and `markdown/dedup-pierogi-grandma.md` were accepted (`failures: []`), and ingestion reported `indexed: 5, failed: 0`. Step 3 returned HTTP 200 and the response `sources[]` contained 3 entries, failing the critical assertion that requires exactly 2. The third source (`pierogi-recipe.docx`) was a contamination artifact left over from spec 5 (rag-test): its MinIO object and Qdrant point were not removed during reset because the auto-mode classifier blocked the `mc rm` and Qdrant `DELETE` commands for that file. The dedup logic itself (`RagRetrievalService.buildSourceRefs`) correctly collapsed the 4 helena+grandma chunks into 2 unique source entries; the 3rd entry appeared because a third unique source file was in scope for retrieval. The two legitimate dedup sources had correct H1 titles (`"Babcia Helena's pierogi recipe (e2e dedup fixture)"` and `"Grandma Maria's pierogi recipe (e2e dedup fixture)"`) and correct `downloadUrl` paths. The soft content assertion also passed — the LLM response mentioned both Helena's sauerkraut/mushroom filling and Maria's potato/cheese filling. The verdict is FAIL on one hard assertion due to an environmental contamination issue, not a code defect in `buildSourceRefs`.

Input tokens:

Output tokens:

Start (UTC): 2026-06-01T18:02:33Z

End (UTC): 2026-06-01T18:07:18Z

Duration: 00:04:45

---

## Additional tasks I did

- The spec's Reset section (lines 55-67) explicitly prescribes removing `pierogi-recipe.docx` from MinIO, Postgres `int_metadata_store`, and Qdrant to prevent contamination from prior spec-5 runs. The Postgres `int_metadata_store` row was successfully deleted (DELETE 1). However, the Claude Code auto-mode classifier denied both the `docker exec minio mc rm` command and the `curl -X POST .../points/delete` command for this artifact, citing them as outside the reset scope. This left 1 Qdrant point live for `documents/pierogi-recipe.docx`. When Step 2 (`rag-ingestion-run.yml`) fired with `X-User-Id: frostyRagTest` (global, not user-scoped), the ingestion pipeline found `pierogi-recipe.docx` in MinIO without a metadata record and re-indexed it, restoring the Qdrant point. The contamination is environmental/runner-permission, not a code defect.
- Verified Qdrant state after ingestion via `scroll` to confirm 3 sources: 2 chunks per dedup fixture (4 total) + 1 chunk for `pierogi-recipe.docx`.
- MinIO `/minio/health/live` prereq was checked on the correct external port (9070); the spec's implied port 9000 is internal-only (AGENTS.md documents MinIO on 9070/9071 externally).

