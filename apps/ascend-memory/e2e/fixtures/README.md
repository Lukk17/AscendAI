# e2e fixtures

AscendMemory operations accept string payloads only — memory text, query strings, user IDs, optional metadata. No
file uploads. The current suite has no fixtures.

Reserved for future tests that might need canary content (e.g. recorded mem0 responses for an air-gapped CI profile
that stubs the embedding backend).
